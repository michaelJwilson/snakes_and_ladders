`snakes_and_ladders.oxi_snakes_and_ladders.max_flow` returns `(value, source_side)` and takes a `reverse` capacity per arc, so the minimum cut Dinic already computes is no longer discarded at the boundary. `snakes_and_ladders.search.maxflow_rust.min_cut` replaces `max_flow` and takes and returns what `snakes_and_ladders.search.maxflow.max_flow` does, leaving the network it is given unmutated; `snakes_and_ladders.search.maxflow.FlowNetwork.as_arrays` is the one reading of the paired-arc layout that crosses.

`alpha_expansion` and `expand` take `backend=`. `Backend.RUST` runs the compiled cut and is **4.82x to 10.76x** the Python one at extents 8 to 32 with 3 to 5 labels, against the 3x the #341 audit expected; it is opt-in and the pure solver stays the oracle, because a degenerate network could attain the same minimum with a different cut. On the seeded fixtures both routes return the same labelling, energy, cycle count and move count. A `set` construction inside `expand`'s per-edge loop, quadratic in the edge count, is hoisted: 3.4% to 10.1% of the Python route on its own.

Root `CLAUDE.md`'s FFI rule no longer says the marshalling has been the dominant term at this boundary; three measurements say it has not been, and `DEV.md` gains **Mitigating an FFI Cost**, stating the four mitigations with the profile each needs before it lands.
