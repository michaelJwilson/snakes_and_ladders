`tests/regression/test_document_labels.py` is green again. It reported three
dangling labels that are a test's own document fixtures, written into
`tmp_path` by the citation integrity checker's tests and naming nothing under
`docs/tex/` by design --- the same reason that module already excluded itself
--- and one real typo: `snakes_and_ladders.likelihood.turbo` cited
`alg:turbo`, and the textbook's label is `alg:turbo-decode`. Found while
validating #492; unrelated to it.
