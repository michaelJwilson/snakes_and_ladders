`RELEASE.md` carries the release procedure in full — what a cut is taken against, the preconditions with their current verdicts, the steps in order, who runs each, and rough upper bounds on what a release costs — and `DEV.md` keeps what a *change* meets and points there. Its numbers are bounds a reader judges a commitment by, never baselines; `tests/regression/test_release_gate.py` now asserts the stated figure-pass bound is not below `snakes_and_ladders.qa.manifest`'s sum, an inequality being the honest guard for a rounded number. `DEV.md`, `INSTALL.md` and `STATUS.md` are corrected against the tree after #490, #492, #521 and #526: the document build renders all 23 manifest entries at 431.8 s rather than 21 at 305.3 s, no stamp narrows any figure selection, the `.inputs` stamp is gone from the figure pipeline's committed set, `documents` is a push-to-`main` job rather than one of the nine required checks, and the branch-protection paragraph records the rename as confirmed and tells a retired check name apart from a skipped one. The critical tier is re-measured on a quiet host.
