The Metropolis accept step has one home, `sample.accept`, which thirteen sites across the samplers, one search and one learned environment now call, and the forward pass one, which `likelihood.forward_backward.sample_path` now shares with the evaluator and is validated by; every chain and every posterior is bitwise what it was.
