The validation harness has one home per seam: `validation.protocol.received()` and `measured(call)` replace each script's hand-written preamble and timed peak, and `Run.peak_bytes` is an `int`, 0 where unmeasured (#1010, N1–N3).
