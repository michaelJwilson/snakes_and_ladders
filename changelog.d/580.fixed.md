`ROADMAP.md`'s numbering is consistent, and `STATUS.md` and `TICKETS.md` track
every milestone it defines. Three milestones were defined and tracked nowhere —
1.5, 2.0 and 3.1 — which failed
`test_the_three_documents_name_the_same_milestones` on `main` and on every
branch that merged it. Milestone 1.5 was a one-line stub with an unclosed bold
marker and no deliverable; milestones 3.1 and 4.1 sat under Stage 2's heading,
a stage number with no stage; and both `STATUS.md` and `TICKETS.md` numbered
Research Extensions Stage 3 where `ROADMAP.md` numbers it Stage 5.

Milestone 2.0 keeps its number rather than being folded into the 2.1--2.3
sequence: it is the definitional milestone the numbered work follows from, and
renumbering it would have rewritten twelve references across the documents,
two test modules and a QA renderer — one of which, `sim/ldpc.py`'s
"Gallager's (1962, §2.2)", cites a section of a paper and not a milestone.
