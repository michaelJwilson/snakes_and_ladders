The names one module imported from another's privates are public where they are defined (`sample.potts_mcmc`'s ten move and sweep kernels, `sample.gibbs.Indexed`, `sample.hmc.gradient_at` and `run_chain`, `sim.topology.from_adjacency` and `leaf_names_of`, `FlowNetwork.forget_arrays`), `sim.fixtures.Fixture` is generic in its params and parses its file once, and the dead surrogate classes, `qa` layout and style helpers, and four unused constants are gone with their tests.
