`PottsGraph.to_rustworkx` and `PottsGraph.from_rustworkx` (#976): rustworkx no longer enters the package process, and `validation.rustworkx` builds its graphs from the edge arrays in a subprocess. rustworkx leaves the `frameworks` extra for `validation-rustworkx`. The in-process cut against networkx is dropped with them: #973's PyMaxflow oracle pins the same cut exactly.
