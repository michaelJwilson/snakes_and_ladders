`sim.potts.energies` sums the edge term as a pairwise reduction outside BLAS, over a row-major gather, and returns the same bits at any BLAS thread count (#1044). As a gemv at the release-q10 rung (5,041 sites, 14,840 edges) one labelling cost 7.0 to 12.2 ms at the host's default BLAS threads and 130 us at one; it now costs 95 to 103 us at either, and the energy moved by up to 4.5e-13 between one and four threads where it now moves by 0. At default threads, SOLVER_BUDGET, median of 3 before and 5 after, load 7 to 9 on 4 cores: annealing 12.44 s to 0.91 s, Swendsen-Wang 12.48 s to 0.55 s, Wolff 12.10 s to 0.20 s, tempering 0.94 s to 0.87 s. Values move from the gemv's by at most 2.1e-14 relative, inside the 1e-12 declared against `likelihood.potts.log_weights` (#341). `learn.potts_nd.PottsNDEnvironment.score` mirrors the new sum, so its bitwise pin against `energies` holds; the recorded expansion energy in `test_alpha_expansion.py` moves by one ulp, 7.1e-15, onto the exactly rounded sum.
