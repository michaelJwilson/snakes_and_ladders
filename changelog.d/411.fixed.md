`docs/tex/generated/method_notes.yaml` no longer states a reason for two pairings that are now tested; the Potts lattice's samplers and optimizers gained tests and kept their untested entries, which `tests/regression/docs/test_problems_tables.py` refuses.
