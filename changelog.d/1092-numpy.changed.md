NumPy at the public API: `hmc.effective_sample_size` returns a NumPy array; `mean_field_log_partition` and `spanning_tree_log_partition` take NumPy and return `float`, their differentiable tensor forms renamed `mean_field_log_partition_torch`, `spanning_tree_log_partition_torch` and `tree_log_partition_torch`; `opt.mixture.responsibilities` is `responsibilities_torch`. A guard refuses a new public tensor outside a torch name (#1092).
