The negative-binomial dispersion solve under a varying exposure keeps the score's `(e mu - y) / (r + e mu)` term, which profiling cancels only at a constant exposure: the fitted dispersion is now the likelihood's maximum at the fitted mean, where it sat a relative 5.6e-4 off on a 1,500-observation draw; fits at a constant exposure are unchanged bitwise.
