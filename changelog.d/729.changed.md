Coverage counts a test only when it judges the science. `infra/coverage_recut.py` recuts a run written with `--cov-context=test` to the tests carrying `end2end` or `oracle`, with `qa` exempt and `search` held to its own floor; CI runs it on the push to `main` and `infra/release.sh` at the release gate, and root `CLAUDE.md` states the rule. The marker scheme changes with it: `simulated_truth` is `end2end`, `mathematical` is `analytic`, `structural` and `edge_case` are retired in favour of `infra` and `smoke`, and `patch`, `backend`, `bug`, `warning` and `snapshot` are registered as the finding axis (issue #729).

The `search` package's tests are audited against the referee each one names, and the three modules no judging test entered gain oracles (issue #729, step 2). Judged coverage with `qa` exempt goes **83.08% to 83.75%** and `search`'s own **87.07% to 89.99%**; the complement --- every kind and finding but `end2end` and `oracle` --- goes 89.31% to **89.29%** over the whole and 88.66% to **88.96%** for `search`, the whole's 0.02 falling to what the judging set took. The floors in `infra/gates.py` are raised to 83.7, 89.9 and 88.9, the whole's unjudged floor standing at 89.2. `search/kernels.py` goes 12.68% to 97.18% against `likelihood.potts.log_weights`, the enumerated ground state and the enumerated Boltzmann marginals; `search/surrogate.py` 79.82% to 97.37% against `enumerate_potts` and a refitted maximized log-likelihood; `search/projection.py` 92.27% to 94.48% against the components a planted draw was made from. Fourteen tests judged against something outside the implementation and marked `smoke` are remarked `oracle` or `end2end`, and one rewrite that added no referee is reverted.

Five codes gain the `end2end` run each lacked, from a planted message at a declared noise to the decoder the package ships (issue #729, step 3). Convolutional: 500 draws at `sigma = 0.5` return the planted message exactly and 2,000 at `sigma = 0.8` give Viterbi 0.0600 and BCJR 0.0630, bracketed by the enumerated weight spectrum's 2.6e-3 and 8.1e-2. Turbo: 0.115000, 0.060833, 0.033333 and 0.013333 at 0, 1, 2 and 3 dB against the 0.115, 0.061, 0.033 and 0.013 `STATUS.md` records. CSS, which has no message to plant: a planted `X` error at `p = 0.05`, its syndrome, and 0.178750 over three seeds of 4,000 trials against the recorded 0.178750 and 2.54x the enumerated degenerate floor. The elementary codes: repetition, single parity check, Hamming and Golay at 0.0535, 0.1975, 0.0435 and 0.0925 against closed forms of 0.0579, 0.1869, 0.0444 and 0.1070, the widest departure 1.2 binomial intervals of 3.0. Reed--Solomon `RS(7, 3)` over a binary channel: 0.06725 against the bounded-distance 0.06496, with all 3,731 draws inside the guarantee recovered and none of the 269 past it right by chance. Judged coverage with `qa` exempt reads **83.89%** and the complement **89.28%**, so the judged floor rises to **83.8**; `sim` goes 77.44% to **78.64%**.

`learn` gains the six referees it lacked (issue #729, step 4). `learn/potts_nd.py`, which pinned its score bitwise to `sim.potts.energies` and never ran a search: a field planted on one labelling, the unique maximum of all 19,683 at nine sites, recovered from eight seeded starts at 9 and 36 sites by a policy whose greedy action is the sweep at rung zero and by `steepest_action`. `learn/relaxed.py`, judged at the corners alone: four interior points of a 40-step ascent against enumeration over 2,187 configurations, the relaxed score to 2.2e-15 of a declared 1e-12 and the gradient to 1.1e-15 of 1e-10, with `estimate_gradient` biased at every point (0.172 to 0.564 of the largest exact component) and pointing along it (cosine 0.9787 to 0.9973). `exact_action_values` against a forward expansion over all 512 trajectories, 2.2e-16. `temporal_difference_targets` against the closed form `gamma = 1` gives, 4.4e-16. `failure_curve` against the binomial enumerated with `math.comb`, 400 starts at five sizes inside the 99.9% band. `arena`'s greedy row against a table written out by hand, 7 of 8 at 3.375 evaluations. Judged coverage with `qa` exempt reads **84.93%** and the complement **89.26%**, so the judged floor rises to **84.9**; `learn` goes 79.61% to **86.62%** and its judged deficit 342 to 191 statements.

`likelihood` closes the nine callables no judging test entered (issue #729, step 4). Three referees: every route `select_device` can take, at the dtype `default_dtype` hands it, against `pruning.log_likelihood` at the tolerance `cross_device_rtol` returns --- 0.0 relative on the float64 routes against 1e-11, 5.38e-08 and 3.23e-08 on the float32 route against 1e-06; `eq:density-evolution` against a scalar recursion written out beside it, bitwise over 60 iterations, and the closed form `min_x x / (1 - (1 - x)^(k-1))^(j-1)`, which the bisection matches to 2.70e-05 and the published (3,6), (4,8) and (3,5) thresholds to 4.66e-05; and `prune_with_matrices` against direct marginalization over the internal states at three branch-length scalings, 3.29e-16 at the widest of a declared 1e-12. Thirteen tests judging against enumeration, a closed form, a published value or the reference while marked `analytic` or `smoke` gain `oracle`, keeping the kind they carried where they also state a property or a contract. Judged coverage with `qa` exempt reads **85.35%** and the complement **89.25%**, so the judged floor rises to **85.3**; `likelihood` goes 89.17% to **91.24%** and its judged deficit 259 to 198 statements.

`opt` closes the four callables no judging test entered (issue #729, step 4). The sampler's marginals against the exact posterior of a Gaussian target: a warm-up built in the test --- 300 proposals at a target of 0.80 --- settles on a step of 0.9952 and a mass diagonal of 0.862 and 2.688 against the marginal inverse variances 0.5 and 2.0, and its 1,200 draws land the marginal means within **2.17** and **0.06** Monte Carlo standard errors of the closed form and the marginal variances within **0.36** and **0.57**, against a declared 3.0; the same chain at the module's own target of 0.65 accepts at 0.665, exactly where it was asked to, with a largest energy error of **1.190e+05** against **27.78**, so `opt/CLAUDE.md`'s rule is asserted rather than restated. `budget.mcnemar` against `scipy.stats.binomtest` over the 168 contingency tables with up to twelve discordant instances either way, 1.84e-16 relative at the widest of a declared 1e-12, and one hand-built comparison's hits, mean gaps and paired p-value by equality. The test-function fixture's six declared minimizers against the published values, each function's own value (at most 1.10e-11) and closed-form gradient (4.19e-05) there and the smallest Hessian eigenvalue (0.40), with a fit from each declared start reaching a declared minimizer at distance 0.0 against the fixture's `at_minimum` of 1e-04. Four tests judging against a closed form while marked `analytic` gain `oracle` and keep it. Judged coverage with `qa` exempt reads **85.49%** and the complement **89.25%**, so the judged floor rises to **85.4**; `opt` goes 85.93% to **87.28%**, `opt/budget.py` 64.35% to **76.52%** and `opt/schedule.py` 76.03% to **81.51%**.

`sim` gains the referees it lacked for its constructions, its parser and its closed forms (issue #729, step 4). `capacity` against the rate-1/2 limits the texts quote --- `eps*` 4.66e-10 from 1/2, `p*` 2.79e-05 from 0.11, `sigma*` 5.88e-06 from 0.9787, against a declared 1e-03 --- and against `scipy.optimize.brentq` on `H(p) = 1 - R`, 4.21e-10 at the widest of five rates. `is_perfect` against the sphere-packing counts the literature prints, equality for Hamming (7,4) and (15,11), Golay (23,12) and the odd repetition codes and strictly under it for the extended ones; `crc_remainder` against the published long division of 11010011101100 by 1011, a four-bit division by hand and a shift-and-xor over Python integers on all 256 eight-bit messages. `galois.field` against the published GF(16) table under `x^4 + x + 1` and Fermat over all 501 nonzero elements of the seven recorded degrees. `gtr.exchangeabilities_from_free` against the closed-form inverse of its gauge, 2.22e-16 of a declared 1e-15. `graph`'s lattices against `rustworkx.generators.grid_graph` and the closed-form triangular degree-6 torus. `newick` against the two trees the format's documentation carries, the rooted one byte for byte. Ten tests judging against a published value or a closed form while marked `analytic` gain `oracle` and keep it. Judged coverage with `qa` exempt reads **86.11%** and the complement **89.27%**, so the judged floor rises to **86.1**; `sim` goes 78.64% to **82.05%**, its judged deficit 400 to 311 statements, with `sim/capacity.py` 23.29% to **73.97%**, `sim/galois.py` 71.13% to **87.63%**, `sim/graph.py` 76.22% to **86.01%** and `sim/elementary_codes.py` 64.52% to **82.80%**.
