A noisy, strongly coupled tiling fixture, `spatio_tiling_noisy/{ci,release}` (21 x 21 and 75 x 75): `spatio_tiling`'s tiles and strengths at J = 1.4 with `0.5 * sim.potts.smoothed_noise` on the field, Gaussian noise averaged over neighbours twice, declared as an optional `noise` block of the `spatio-tiling` model. `SpatioTilingParams.redrawn` draws held-out instances, and the release file records twenty, on which alpha-expansion's gap to the TRW-S bound averages 10.19 against ICM's 2,087.35 from the field's argmax (35 SE on the paired difference). `search.potts_starts.held_tiles` states the recovery bound site by site, so it holds on a noisy field; on a noise-free one it is `recovery_bound`'s.
