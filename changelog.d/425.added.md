`infra/new_worktree.sh`, the agent worktree convention as one command: the branch from a named base, the shared `.venv` symlink, the gitignored compiled extension, an import check that refuses a resolve outside the new worktree, and the `PYTHONPATH` to export. It replaces five steps repeated in every agent brief that failed four times on 2026-09-08 — a sibling worktree's code imported and judged (#401), 164 collection errors from a real `.venv` directory (#404), and two worktrees without the extension.
