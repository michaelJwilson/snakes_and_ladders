BlackJAX as an oracle for the HMC integrators and a timing reference for the sampler (#963): the `validation-blackjax` extra, `snakes_and_ladders.validation.blackjax` and its script, run in a subprocess so JAX never enters the package process. Leapfrog and the Yoshida triple jump agree with BlackJAX's integrator on the same composition within 1e-12 over 25 steps; 5,000-transition chains from both centre within 4 Monte Carlo standard errors and accept within 0.01 of each other. Goal tests hold `sample.hmc` to BlackJAX's runtime for 1,000 transitions at d = 10², 10³ and 10⁴, and `tests/benchmarks/test_hmc_blackjax_bench.py` times the pair.
