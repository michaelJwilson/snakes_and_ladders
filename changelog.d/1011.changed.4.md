`sample.relabel` imports no torch (#1011, step 2): every function takes array-likes and returns `np.ndarray`, a tensor is detached and moved to the host on entry, found by its `detach` method, and `EmissionFamily` is imported for its annotation alone. `allocation_draws` imports torch on the call, since each family's `log_density` is a tensor function. Importing the module leaves torch unloaded, at 77 MiB peak RSS where it took 663 MiB (median of 5 fresh interpreters). On the suite's seeded fixtures, 228 recorded outputs (permutations, per-pass objectives, SJW weights, allocation probabilities and allocations) are bitwise unchanged. `relabel` landed in #964 after release 0.3.0, so no release carried the tensor-typed `allocation_draws` signature and no shim is kept.
