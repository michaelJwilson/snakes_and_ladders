Restore the HMM loader's two size guards, deleted with the field beside them
(#667). `n_states` and `n_symbols` below 2 were refused; flattening the loader
onto `lengths` removed those checks along with `sequence_length must be >= 2`,
and a one-state fixture whose arrays are shaped to match was accepted. The
tests that covered them still failed, but on `_stochastic`'s shape message
rather than on the guard, which is how the loss stayed invisible: the
parametrization asserts the message, and a message that changed reads the same
as a message that moved.

The third case edited `sequence_length: 15`, a field `hmm/ci.yaml` no longer
spells, so it raised nothing at all. It now edits the first entry of `lengths`
and asserts the shortest-chain refusal. An empty batch is refused by its own
test rather than by a substitution, since emptying the list means replacing the
whole block.
