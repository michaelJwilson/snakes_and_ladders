`search.icm.SweepOrder` gains `CHECKERBOARD`, the classes of a greedy colouring (`search.icm.colouring`, compiled, 42 us against the Python loop's 11.0 ms at 75 x 75) swept one after another on both backends, and `RESIDUAL`, sites by descending gain of their best move, recomputed per sweep, on the Python backend. From uniform starts on held-out `spatio_tiling` fields, ten a size, every order beats `INDEX`'s gap to the TRW-S bound at 41 x 41 and 75 x 75 by 3.3 to 4.3 SE (75 x 75: 264.8 against 58.8 checkerboard, 48.4 random, 44.3 residual); from the field's argmax none differs by more than 1.2 SE. The default stays `INDEX` here.
