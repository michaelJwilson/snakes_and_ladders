Cluster moves against the graph cuts (#1041): `sample.potts_mcmc` gains two move sets that read the field, `ghost-spin` (the field as bonds to one ghost site per label, no accept step) and `label-directed` (Fortuin-Kasteleyn clusters proposed onto one cycled label, Metropolis-Hastings), and `cluster_tempering` (Swendsen-Wang replicas with a Houdayer move across two temperatures); each is held to the enumerated Gibbs law at 3x3 and three labels. `search.cluster_moves` runs the two expansion hybrids and the cluster tempering as ground-state arms; `search.ground_state.ends_labelling` shows a `spatio_only` optimum uses only the ladder's two end classes; `search.potts_starts.RunStart` runs any arm as a start. `qa.potts_clusters` writes the diagnosis and the arms to `docs/nb/data/potts_clusters.json`, which `potts_starts.ipynb` reports.
