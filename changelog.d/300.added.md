The coupled spatio-sequential model of #290 has declared parameters, a simulator under one generator (`sim.spatio_sequential`), and an enumeration oracle for its evidence and posteriors (`likelihood.spatio_sequential`), pinned against the factor graph and against the per-class forward recursion.
