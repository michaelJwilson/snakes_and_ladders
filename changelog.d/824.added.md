`sample.smc.smc_topologies` builds trees by coalescence under partial-likelihood weights and returns the particles, their weights, an evidence estimate and its cost in partial-likelihood evaluations; `split_support` reads a posterior split support off the particles. The target it samples is stated exactly and enumerated at five taxa, where the evidence estimate is unbiased for the exact mean likelihood and the particles follow the exact law (#824).
