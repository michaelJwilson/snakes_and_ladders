The two QA figures no document cited are cited. `sim_problem_sizes` becomes
Table~`tab:sim-problem-sizes` in the paper's simulation section, beside the
comparison against the closed form whose seeds and Monte Carlo tolerances it
tabulates; `topology_accuracy` becomes Figure~`fig:topology-accuracy` in the
paper's search section, where enumeration referees whether the search found
the best tree for the data and simulated truth referees whether that tree is
the one that generated it. `topology_accuracy`'s caption drops the site count
at which the sweep first clears the accuracy requirement and the count of
replicates recovering the topology exactly at either end: both are
discontinuous in 48 L-BFGS fits taken at the margin, which `docs/CLAUDE.md`
refuses to publish. What it states instead --- the mean meets the requirement
at the longest alignment swept and misses it at the shortest --- is asserted
by its release-tier test. Its 124.0 s render is over `CITED_RENDER_CAP` and is
waived under #509, the cost attributed to L-BFGS branch-length fitting by #506
and not reduced here.
