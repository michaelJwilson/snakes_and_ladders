`README.md`, `DEV.md` and `STATUS.md` corrected against the tree: `DEV.md`'s layout names `sample/` and no longer lists the samplers, schedules and initializers under `opt/` and `search/`; `learn/`'s import rule names its exceptions; the `documents` job runs on a pull request touching an input; STATUS's row 4.1 says the Aim store landed; and the three documents that call `docs/textbook.pdf` committed say it has been missing since f16a26ab (#847).
