`snakes_and_ladders.likelihood.pruning_rust.PruningProblem` holds an alignment, `k` and `pi`
across pruning passes, so a caller scoring many topologies against one
alignment marshals the alignment once rather than per pass. It returns the
same `float` as `pruning_rust.log_likelihood`, which stays as it was.
