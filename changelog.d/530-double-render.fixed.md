The release gate renders each QA figure once. `infra/release.sh` ran `snakes_and_ladders.qa.build --all --check` and then `infra/build_documents.sh`, whose own figure pass selects the union of what the two documents cite; since issue #492 left every manifest entry cited, the two selections are the same 23 stems, verified by `--list`. The stamps that once let the second pass skip most of the work were deleted by issue #490, so the gate paid the manifest's declared 431.8 s twice, 863.6 s in all.

`infra/build_documents.sh` takes `--no-figures`, and the gate passes it: a `--check` that passed has proved the committed bytes are what a render produces, so the second pass could only write them back. The comparison still runs first — that ordering exists so it is not made against bytes the build itself wrote (issue #484) — and the build after it now writes no figure at all. The `documents` job on the push to `main` calls the script with no arguments and renders as before. A failed comparison names the remedy, `infra/build_documents.sh` with no arguments; the gate does not run it, having already failed.
