The negative-binomial M step under an exposure runs in the compiled kernel (`negative_binomial_dispersions_exposed`): 2.99x the per-state torch solve at 20,000 counts and four states, agreeing to #648's floor; counts wider than `EXPOSED_TAIL_RATIO` (8) times the observations fall back to the torch solve, where the kernel stops clearing 2x (R4).
