`search.mixture_starts.best_of(name, n)` runs a stochastic start `n` times on sub-streams of the cell's generator and hands over the best seeding at `n` times the cost; the starts notebook adds a best-of-5 row group beside the single-seed rows at the same budget.
