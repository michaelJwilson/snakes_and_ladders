`infra/duplication_survey.py`, `infra/appraise_structures.py` and `infra/appraise_seams.py` with their three tests, 1,446 lines: a regex survey of overlap whose pinned rows moved with edits to the file that held them. The audit is a reading, recorded in a dated review. `tests/regression/test_duplication_guards.py` keeps its thirteen guards, which refuse the next copy rather than count the last one. The three `search` shims #779 left (`gym`, `rl`, `surrogate`) are removed, as that issue said they would be in the release after 0.3.0.
