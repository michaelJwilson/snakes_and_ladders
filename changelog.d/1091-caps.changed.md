One loop cap and one tolerance name: `max_iterations` replaces `max_sweeps` (`iterated_conditional_modes`, `value_iteration`), `max_cycles` (`alpha_expansion`, `alpha_beta_swap`), `max_rounds` (the four ladder adapters and `LadderCalibration`) and `dual_bound`'s `iterations`; `tolerance` replaces `fit`'s and `fit_from`'s `gradient_tolerance`. `dual_bound` takes the `tolerance` it hard-coded, default unchanged at 1e-12. An old keyword raises `TypeError` (#1091).
