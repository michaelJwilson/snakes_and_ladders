The notebook check no longer decides from a digest whether it runs: `infra/check_notebooks.py` re-executes every notebook under `docs/nb/` and states the set before executing any of it. `turbo.ipynb`, `phylo_tree.ipynb` and `potts_chain.ipynb` print the tolerance each agreement clears rather than the near-zero sample that carried it, which did not survive a rebuild on another machine.
