`opt.split_merge` imports no torch (#1011): `merge_criterion`, `split_criterion` and `candidate_moves` take and return `np.ndarray` where they took `torch.Tensor`, and `split_and_merge` takes `observations` as an array. The E and M steps it ran through the families' torch API move behind three new `opt.emission_mixture` functions, `log_densities`, `responsibilities_at` and `partial_expectation_maximization`, and `expectation_maximization` accepts its starting `weights` as an array. `opt.split_merge` landed in #904 after release 0.3.0, so no shim is kept. On the `emission_mixture/ci` planted start and four data starts, every step, final responsibility, weight and log-likelihood is bitwise unchanged, as are all 24 split seeds and all 32 candidate orderings; `merge_criterion` is bitwise in 27 of 32 evaluations and within 5.0e-16 relative, `split_criterion` within 3.6e-14 relative, both against a declared 1e-12, where NumPy's column sum reorders torch's. Import time and peak RSS do not move (1,539 ms and 622.6 MB, was 1,387 ms and 622.3 MB): `emissions` still loads torch.
