`tests/regression/test_sandbox.py::test_the_sandbox_states_its_rules_where_the_root_says_they_live` passes again. It read three rule sentences out of `python/snakes_and_ladders/sandbox/CLAUDE.md`; a trim reworded one and removed the other, so the guard failed on `main`. It now reads the two rules the file states locally, and the package-root rule is left to root `CLAUDE.md`'s Package Surface section, which `test_the_package_root_does_not_re_export_the_sandbox` already asserts off the source tree rather than off prose.
