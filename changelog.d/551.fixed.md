`search.potts_mcmc` took an external field shared by every site, so a per-site field --- which every `potts_spots` instance declares --- raised `IndexError` inside the heat-bath sweep. The sweeps, the cluster recolouring and both chain drivers now widen the field through `sim.potts.site_field`, and a cluster's accept step sums the field difference over its own members.
