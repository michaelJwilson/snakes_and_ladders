`opt.emission_mixture.expectation_maximization` takes a `schedule`: deterministic annealing EM, responsibilities tempered along a `TempSchedule` that ends at one before the tolerance loop; `schedule=None` is bitwise the plain fit. The starts notebook offers it as a polish beside plain EM.
