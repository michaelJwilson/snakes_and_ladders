`emission_mixture_starts.ipynb` gains a `gibbs-anneal` start: heat-bath sweeps over the component assignments at a falling temperature on the count-pair likelihood itself, the components re-estimated at each sweep's hard assignments, through `opt.emission_mixture.anneal_assignments` and `search.mixture_starts.annealed_gibbs_seeding`, under the same schedule as the Hamiltonian `anneal`.
