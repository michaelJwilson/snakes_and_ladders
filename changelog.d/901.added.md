`emission_mixture_starts.ipynb` gains a `gibbs-anneal` start: heat-bath sweeps over the component assignments at a falling temperature on the count-pair likelihood itself, the components re-estimated at each sweep's hard assignments, through `opt.emission_mixture.anneal_assignments` and `search.mixture_starts.annealed_gibbs_seeding`, under the same schedule as the Hamiltonian `anneal`. Over five seeds on `emission_mixture/stress` it hands over the smallest gap, 518.1 ± 169.4 nats, and ends 0.7 ± 21.6 nats above the generating parameters, 15.8 below `anneal`. The gap figure drops its title, reads its gap on a symmetric-log axis, splits into the starts ending within a nat of the generating parameters or past them and the rest, and draws its spread bands only under `SHOW_BANDS = True`.
