The per-pull-request test job bounds its fallback rather than running the whole suite for any change it cannot attribute, and carries a timeout; the whole suite runs on the push to `main` and at the release gate. A change touching a likelihood kernel, the Rust sources or a lockfile refuses the bound.
