Residual belief propagation is the sixth `MessageSchedule`, `residual`: a heap over the factors keyed on the largest residual their inputs last saw, so each send goes where the graph is still moving. `sum_product` and `max_product` take it by name; the seam gains `adaptive` and `sweep_length`, and the runner feeds each applied step's residual back to an adaptive schedule. The same Bethe fixed point as flooding and sequential, pinned against the reference flooding on the loopy lattice (#825).
