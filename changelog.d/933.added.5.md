`SpatioSequentialParams(shared_emissions=True)` holds one emission family for every class, re-estimated on every class's block pooled; `seed_emissions(..., seed_counts=True)` and `graph_burn_in(..., seed_counts=True)` seed negative-binomial, beta-binomial and `IndependentCountPair` families by `Emission_Mixture++` in rate space (R6).
