A notebook's pull-request diff is collapsed: `.gitattributes` marks `docs/nb/*.ipynb` `linguist-generated`, so GitHub folds the JSON behind "Load diff" and the review reads the re-executed outputs the `notebooks` job compares (#889).
