Three defects the design audit found (#856): the Gaussian mixture EM refuses an unconverged M step and reports `at_boundary` as the emission mixture does; `search.ground_state` carries each run on its `Outcome` instead of a module-level list that was empty at `workers > 1`; one rank rule in `sim.count_pairs.split_covariate` decides the count-pair covariate for both simulators.
