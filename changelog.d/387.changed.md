One enumeration behind the oracles: `sal.enumeration.configurations` is the product space under the cap, `posterior`, `site_marginals` and `argmax` its reductions, and `enumerate_potts`, `enumerate_hidden_paths`, `enumerate_spatio_sequential`, `learn.potts.enumerate_configurations`, `learn.hmm.enumerate_paths` and the three `optimum` helpers call it with their signatures and values unchanged; the two `learn` enumerators gain the cap they lacked (issue #387).
