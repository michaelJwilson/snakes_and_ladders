`search.bifurcation` imports torch only on its `Backend.TORCH` route, so importing `search.bifurcation` or `search.ground_state` no longer loads torch: 1,783 ms and 621.7 MB fall to 109 ms and 28.4 MB, and 1,838 ms and 625.9 MB to 215 ms and 38.7 MB (median of 5 fresh interpreters, under load). The arithmetic and signatures are unchanged (#1011).
