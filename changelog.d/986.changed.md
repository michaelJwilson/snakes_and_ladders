Compiled routes to 2x past the external goals (#986), each the new default with the previous route kept as its oracle: `baum_welch` and one-dimensional `expectation_maximization` stream their E and M steps through Rust (Baum-Welch at 10^6 positions peaks at 0.3 MB, was 391 MB); `hmc.sample` runs a plain chain on a `DeclaredGaussian` in one call (1.9 ms at d = 100, was 1.10 s); `hmc.gradient_at` reads a `DeclaredGradient`'s closed form, which `GaussianMixtureObjective` states; the Ising cut runs a Boykov-Kolmogorov engine with terminal residuals on the nodes; `sample.potts_mcmc.bond_roots` exposes the Rust union-find; the REINFORCE surrogate and PPO objective score each state once and reduce in one expression; `GraphSurrogate` sums neighbours and pools by sparse products. The release profile is fat LTO over one codegen unit, and the goals' ratio is 0.55.
