A Hamiltonian chain whose trajectory drives a family's parameter out of its domain rejects that proposal, as it rejects one of infinite energy, rather than stopping on the family's refusal: the families raise `emissions.ParameterDomainError`, a `ValueError`, for a non-positive scale, rate or shape, and `sample.hmc` catches it alone.
