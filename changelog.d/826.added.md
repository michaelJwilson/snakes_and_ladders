Polar codes are a problem row: `sim.polar` (the transform, the erasure and Gaussian constructions, the Reed--Muller rule, `PolarParams`) and `likelihood.polar` (successive cancellation, the list) are promoted out of `sandbox/` bitwise with their 22 tests, the `polar` fixture joins the registry at three tiers, and `decode_scl(..., crc=)` is CRC-aided list decoding: the best survivor whose check passes, the exhaustive list at `N = 16` returning the outer code's enumerated maximum-likelihood codeword (#826).
