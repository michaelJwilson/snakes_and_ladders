A third coverage guard, the complement of the judged set --- every kind and finding but `end2end` and `oracle` --- recut from the same run with its own floors, and `infra/coverage_recut.py --functions`, which lists per package the public callables no judging test enters (issue #732).
