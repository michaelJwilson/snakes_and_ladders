Fixed the root-detection assertion in `test_the_search_finds_the_generating_tree_and_rejects_a_worse_one`: it now walks every node of both fitted trees via `snakes_and_ladders.sim.tree.preorder` instead of checking only the two root objects, unblocking `infra/release.sh`'s release gate.
