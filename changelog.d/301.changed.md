`import snakes_and_ladders as sal` reaches every submodule by attribute, each imported on first access, and every document says `sal.<module>` where it said the long name; `infra/abbreviate_package.py --check` keeps it so.
