`likelihood.surrogate.least_squares_lengths` returns one answer bitwise on repeated calls in a process (#1026). `torch.linalg.lstsq`'s CPU default, MKL `gelsy`, returned 17 to 42 distinct solutions in 100 to 1,000 calls on one 10x7 system, and 9 of the 15 five-taxon topologies' lengths differed across three repeats; the fit takes no derivative, so it is a NumPy `gelsd` solve now and returns `np.ndarray` where it returned `torch.Tensor`. `PlugInLikelihood.lengths` and `BlockFrequencyBound.lengths` still return a tensor. Against the old values the lengths move by at most 3.7e-16 absolute over the 120 topologies of `tree_search/ci` and `tree_search/stress`, inside the old solver's own run-to-run spread of 4.4e-16.
