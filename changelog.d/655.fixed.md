Three checks that had stopped saying what they meant are restated (#654's
merge run, four failures in the tier a pull request does not run).

`test_fixture_registry.py` still parsed `PROBLEMS.md` for fixture *paths*.
#640 made the catalogue minimal and left it a fixture **key**, so the regex
matched nothing and both directions of the registry check passed vacuously or
failed outright. They now read the Key column and resolve each key through the
registry, which is the one place a path is stated.

`test_structure_survey.py` asserted that the sentence "derives a compressed
layout per call" appears nowhere. That sentence is the survey's vocabulary for
the finding, not a name for the one class that fixed it, so
`search.maxflow.FlowNetwork` raising it — real, open as #642 — read as the
`PottsGraph` fix regressing. The absence is now asserted of `PottsGraph`, and
the live finding is asserted present until #642 lands.

`test_torch_patterns.py` drew its operand from an unseeded `torch.rand` under
a bitwise assertion, so it failed on a draw nobody chose. The draw is seeded,
and the assertion is the declared float64 tolerance: the product is bitwise on
this host over 200 seeded draws at every thread count from 1 to 8, and a BLAS
that dispatches a different kernel for a stride-transposed operand sums in a
different order, which is the reduction-order case the tolerance rule covers.
