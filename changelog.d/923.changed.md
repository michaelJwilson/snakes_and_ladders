The ground-state solver `gibbs-T0` is `icm-random` (`search.ground_state.run_icm_random`): ICM in a random sweep order, now on the compiled sweep (`sample.kernels.icm_sweeps_ordered`, bitwise the Python sweep, 38.7x at 71x71 and q = 10). The comparison anneals Swendsen-Wang on its compiled pass (`anneal_potts(cluster_backend=Backend.RUST)`, a chain of the same law; 9.5x), and `potts_starts.ipynb` returns to 1,000 sweeps a solver.
