The release template audits the tree against what the repository declares,
by reading: a Principles section over every bulleted rule of the ten
`CLAUDE.md` files (206 on 2026-09-19, 60 enforced by no test), an Intents
section with one box per milestone carrying `ROADMAP.md`'s validation
sentence, a Seams section over every `Protocol` and `ABC` (14, 3 pinned by a
guard), and a Textbook section read from the document's sections rather than
from a list of boxes, which was already one short. Four guards that matched a
string for a judgement are deleted: the restatement, measurement and Expected
Reader checks in `test_claude_md_pointers.py`, and the box-per-problem check
in `docs/test_document_orphans.py` with the checklist half of
`infra/document_orphans.py`. `RELEASE.md` and the template agree on
`infra/ledgers.sh` and on the one-commit cut in place of the `dev` precondition.
