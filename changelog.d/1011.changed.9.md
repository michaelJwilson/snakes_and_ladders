`likelihood.blocks`, `likelihood.features`, `likelihood.mixture_assignments` and `likelihood.surrogate` import no torch (#1011, step 2 likelihood). `block_frequency_interval` and `site_log_likelihood_extremes` take array-likes and return `float` ends in `Interval` and `Extremes`; `tree_features`, `tree_tokens`, `lattice_features` and `lattice_tokens` return `float64` `np.ndarray`s; `PlugInLikelihood.lengths` and `BlockFrequencyBound.lengths` return `np.ndarray`; `decoupled_ground_energy`, `decoupled_log_partition` and `saturated_log_partition` take array-likes and return `float`. Each module converts once where it calls a tensor function the fits differentiate: `pruning_torch.log_likelihood` and `transition_probabilities`, an emission family's `log_density`, and the mean-field bound, which with the spanning-tree bound stays differentiable in torch. `learn.ranking` turns the arrays into tensors where they meet the learned models. `pruning_common` imports torch only inside `leaf_indicator`, so `pruning_rust` no longer loads it either. `hmm_paths` and `spatio_sequential_rust` drop their own torch import and still load it through `sim.hmm` and `sim.count_pairs`. None of these modules was in release 0.3.0, so no shim is kept. Of 3,102 recorded outputs on the suite's seeded fixtures, 3,084 are bitwise unchanged; the other 18 are the three `O(N + E)` lattice bounds and the features and offsets built from them, within 2.6e-16 relative where NumPy's sums reorder torch's (at most 8.7e-18 absolute on per-site field means near zero). `pruning_torch` and `pruning_analytic` values and gradients are bitwise unchanged on four tree fixtures. Median of 5 fresh interpreters, under load: importing `blocks`, `features`, `mixture_assignments`, `surrogate` or `pruning_rust` takes 111 to 162 ms at 26.6 to 28.8 MB peak RSS, was 1,458 to 1,494 ms at 621.0 to 622.0 MB.
