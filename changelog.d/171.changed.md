Promoted the HMM from an `sal.opt` fitting fixture to a first-class
`sal.sim.hmm` generator that retains the hidden state path alongside the
emitted observations, on the footing the tree simulator already has.
