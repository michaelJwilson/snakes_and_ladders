rustworkx as a validation framework (#976): the `validation-rustworkx` extra, `sal.validation.rustworkx` and its script, run in a subprocess. `tests/validation/test_rustworkx.py` carries the generator, multigraph and topology-isomorphism pins over from the two in-process modules it replaces, and adds one: the Swendsen--Wang union-find partitions a bond mask exactly as `connected_components` does at 16², 71² and 142². Runtime goals hold the union-find to rustworkx's time at 142² and 284²; `tests/benchmarks/test_cluster_labels_rustworkx_bench.py` times the pair and weighs both.
