Three referential folds inside `opt/`, each checked bitwise over 226 digests of `initial`, `constrain`, `theta_from`, `theta_from_truth`, a full `fit` and the expectation-maximization traces: the Potts chain and lattice inherit `_PottsObjectiveBase`'s four `theta` methods, the six HMM objectives and the Gaussian mixture call `opt.constrain`'s `positive`, `free_from_positive`, the new `probability`, `free_from_probability` and one `initialize.quantile_locations` where each inlined them, and `baum_welch_family`, `opt.mixture` and `opt.emission_mixture` alternate through one `opt.em.em_loop` while keeping their own entry points, defaults, result types and oracle pins.
