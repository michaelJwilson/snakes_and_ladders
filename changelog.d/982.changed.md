The per-PR tier collects 3,877 tests, down from 4,076 (−199), over 82,515 lines under `tests/` excluding benchmarks, down from 83,784 (tracked `.py` files, recounted at `b652932b`): duplicated likelihood, QA and sandbox tests are folded into parametrised tables (`ROUTES` in `tests/regression/likelihood/conftest.py`, `SCRIPTS` in `tests/regression/qa/test_qa_runner.py`); the goals into `RUNTIME_GOALS` and `MEMORY_GOALS` in `tests/validation/test_goals.py`; eight absent-outside-owner guards into `ABSENT` in `tests/regression/test_duplication_guards.py`; the sampler recovery assertions into `tests/_posteriors.py`; and each dropped test's claim is asserted against the same referee in the test `infra/ladder.py` or the surviving module names (#982).
