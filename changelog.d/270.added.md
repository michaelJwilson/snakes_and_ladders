A discrete search result states its support (`search.support`): the neighbourhood weight and margin under a move set, the enumerated weight over every topology where it fits, and Felsenstein's bootstrap per split, each named as what it is and pinned where enumeration reaches.
