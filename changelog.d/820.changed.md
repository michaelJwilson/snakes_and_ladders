`reinforce`, `actor_critic`, `ppo` and `TrainingBudget` take `stop_at_local_optimum` (default `True`, bitwise the arena before it); under `False` an episode runs past a local optimum and the greedy row is `greedy_restarts`, hill climbing restarted until the decision budget is spent. Measured on the chain and the 7-taxon fixture: the rule moves restarted greedy to 81 of 81 and every tree row's escape from 0 to 8 or 9 of 9, and no trained policy beats restarted greedy on either (#820).
