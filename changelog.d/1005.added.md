`BranchLengthObjective` and `SubstitutionModelObjective` take `backend=Backend.JAX`: the value and gradient come from one `jit`-compiled pruning program per topology (`likelihood.pruning_jax`), within `1e-11` relative of the PyTorch route. The default stays `Backend.TORCH` until the stress-size measurement decides it.
