Throughput rules in `CLAUDE.md` and `DEV.md`: one agent works at a time and has the whole host; tickets of one plan shape land as one pull request; local validation before a push is the changed-file lint, the type check and the critical tier, with the pull request's CI as the final validation; review starts from `infra/review_gates.sh`; a test over the per-PR duration cap moves to the `release` tier.
