One name per log quantity: `log_partition` on `LogPartition` (was `log_z`, and `rung_log_partition` was `rung_log_z`) and on `BeliefPropagationResult` (was `bethe_log_partition`); `log_evidence` on `PathEnumeration` (was `log_likelihood`) and on the ragged `Posteriors`, whose fields are `log_posterior`, `log_counts` and `log_evidence` (were `gamma`, `counts`, `evidence`). `max_product` returns a `MaxMarginals` whose `map_log_weight` is the MAP's log-weight, which `Marginals.log_partition` carried before; both share `Beliefs` (#1090).
