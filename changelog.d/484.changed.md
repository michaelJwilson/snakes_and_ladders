The release gate renders every figure, cited and uncited, and fails naming any whose bytes do not reproduce. It runs before the document build, which renders a stale cited figure into `docs/tex/figures/` and would otherwise supply the bytes the comparison is against. No stamp decides what is checked: the stamps were wrong on 476 of 476 decisions (#476).
