Annealed Swendsen-Wang and Wolff move sets on `search.potts_mcmc.anneal_potts`, with the cluster-size, field-accept-rate and lattice-spanning counters they are measured by; `search.alpha_expansion.alpha_beta_swap`; and `search.ground_state`, which builds the `spatio_only` rungs and scores a labelling by its energy and by the parameters that generated the field.
