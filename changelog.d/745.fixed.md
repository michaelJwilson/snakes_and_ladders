`main` is green on the push-to-`main` tier, which had failed on every push since 2026-09-18 (#699). Four failures: the API map's stem label `sec:api` is defined in the committed `docs/tex/api_map.tex` wrapper, so the label `tests/regression/docs/test_api_map.py` cites resolves in the tree CI reads rather than only in the generated file `.gitignore` excludes; the kernel-survey pool guard asserts that `coupled`, `pruning` and `sampling` take the rayon pool and `count_pairs` and `ragged` do not, instead of the closed set #715's two max-flow kernels broke, and carries `critical` so the per-PR gate owns it (#745); the problem-marker guard reads the problem markers an author wrote on an item beside the ones the collection hook derives, which is the selection `-m <key>` collects, so the four hand-marked `frustrated_lattice` items stop being reported as defective; and the three `oracle` tests #735 dropped as implied by a neighbour are restored, with the ladder warm-up its cached helper shared across the judged boundary run again by the test that judges it. Judged coverage returns to 83.75% whole and 89.99% for `search`, against floors of 83.7 and 89.9, with `opt` 85.93% to 86.42%.
