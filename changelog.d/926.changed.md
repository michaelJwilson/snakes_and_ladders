The runtime band has one reader, `search.mixture_starts.curve_band`: `gap_band` reads a mixture trial through it, and `qa.starts.curve_band`, the second copy, is gone; import it from `search.mixture_starts`.
