`backend.twin(name, backend, __name__)` refuses a backend other than PYTHON or RUST and imports the module's `<module>_rust` twin at call time; the eight sites in `search.maxflow`, `sim.count_pairs`, `likelihood.pruning`, `likelihood.convolutional`, `likelihood.message_passing` and `likelihood.spatio_sequential` that wrote the refusal, the local import and the cycle comment by hand call it, with every refusal message unchanged (#1010, N6).
