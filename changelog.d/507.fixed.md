`docs/nb/turbo.ipynb` reports its two BCJR agreements against the tolerance `tests/regression/likelihood/test_convolutional.py` pins them at, instead of printing a 1e-14 residual whose last digits moved between machines and failed the `notebooks` job.
