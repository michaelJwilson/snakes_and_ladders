`qa.starts.gap_panels`, `curve_band` and `start_styles`: the gap-against-runtime figure `emission_mixture_starts` and `potts_starts` both draw, one of fourteen colours per start and no dashed lines, the runtime from 0.01 s, the gap in nats, legends ordered by final gap, and spread bands off by default. `emission_mixture_starts` draws only the starts ending within one nat of the generating parameters or past them.
