Six backend refusals in `sample.potts_mcmc`, `sample.gibbs` and `search.alpha_expansion` go through `backend.refuse_backend`, so their message reads "<name> runs on <allowed>, not <backend>" as every other refusal does (#1010, E3).
