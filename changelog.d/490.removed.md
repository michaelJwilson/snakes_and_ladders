The figure and notebook input cache: `snakes_and_ladders.inputs`' `source_fingerprint`, `read_stamp` and `write_stamp`, all 29 committed `.inputs` stamps (23 beside `docs/tex/figures/`, 6 beside `docs/nb/`), the staleness paths in `snakes_and_ladders.qa.build` and `infra/check_notebooks.py`, and `stamps_match_the_tree` in `infra/review_gates.sh`, which had not been a gate since #456.

**476 decisions, every one a false positive, no figure byte moved**, at a cost of ~48 minutes in one day (#476). What each stamp nominally provided is provided by running the check rather than predicting it: `infra/release.sh` renders every figure, cited and uncited, and compares bytes (#484), and the `notebooks` job executes every notebook under `docs/nb/` (#480). A documents build now renders all 21 cited figures, 305.3 s by the manifest's declared times, on the push to `main` where the job runs.

`library_versions` stays: a baseline record is a function of the installed `numpy`, `scipy` and `torch`, which is not a question about figure staleness. Its default — the drawing libraries — went with the figures, so the caller now names its own list.
