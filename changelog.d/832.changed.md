Sixteen `load_<problem>_params(path)` loaders are one `sal.fixtures.load_params(path, Kind)`, behind a `Params` protocol: each dataclass carries its `required_fields` and a `from_declared(declared, path)` classmethod holding the validation its loader held, with no field, default or message changed, and every fixture at every tier loads field-for-field equal to the record the old loader returned. `sim.fixtures.LOADERS` is `PARAMS`, model name to class (#832).
