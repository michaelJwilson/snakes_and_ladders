One Potts model in code (#277). `PottsGraph.compressed_adjacency()` is the only adjacency: `search.potts_mcmc._adjacency`, `search.potts_mcmc_rust.flatten_adjacency` and the open-coded builders in `ground_state`, `alpha_expansion`, `spatio_sequential` and `sim.potts` are retired, the Python sweeps index its rows, and a guard in `tests/regression/test_duplication_guards.py` fails a second builder. The vectorized `energies(graph, field, states)` moves to `sim.potts`, where the simulator can reach it, and is what `alpha_expansion.energy` and `maxflow.energy` score one labelling with: 2.89 ms to 0.82 ms on 32x32 at 64 configurations, and unchanged against the fastest implementation it replaces. `sim.potts.heat_bath_log_weights` is the conditional the vectorized simulator, the sequential sampler and the Rust kernel share; the three loops stay separate. Every chain, configuration and labelling is bitwise unchanged at the same seeds; a reported energy moves by at most 3.7e-16 relative, the edge sum now being a dot product.
