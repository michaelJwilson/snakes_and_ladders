`SEAMS.md` and `infra/seams_survey.py` are deleted. Every `Protocol` the package declares is found at import and every implementer subclasses it explicitly, so the declaration is the record and a hand-written catalogue beside it was a second list to keep in step. Two things the ledger computed do not follow from the class tree and moved into `infra/gate_new_seams.py`, which is where they were used: the consuming-module count, a property of the import graph, and the reason a seam under the three-consumer rule is kept, which now stands in the declaring class's own docstring behind `Under the seam rule:`. `CHECKS.md` is untouched — it is read from the tests' kind markers, which have no home in a class hierarchy.
