The baseline recomputation compares recorded floats within a stated relative
tolerance rather than bitwise. `maximized_log_likelihood` is 45 L-BFGS fits,
which a different BLAS ordering moves in the last digits; exact equality
failed a pull request that changed no code. Every other recorded value, and
every non-float field, keeps exact comparison.
