The stress-tier profile now covers every problem family, the codes included — they had no workload, so nothing ranked them — and one cut is taken from it. `pruning_torch.log_likelihood` hoists its post-order out of the evaluation: a topology fixes the traversal, so the schedule is built once per topology and the evaluation is a flat loop over it, where it was a Python frame and two dictionary lookups per node per evaluation. Value and gradient at 50 taxa x 2,000 sites go from 26.287 / 27.194 ms to 24.032 / 24.367 ms (**1.09x**, 2.54 ms), and a 20-evaluation NNI search at 20 taxa from 19.40 / 17.94 s to 18.36 / 17.18 s; the log-likelihood, its gradient and the `rescale=False` branch are **bitwise** unchanged at 4, 6, 10 and 20 taxa. The profile had attributed 17.5% to the post-order and the realized saving is 4.8%, because `cProfile` charges a frame per call: `STATUS.md` records the correction beside the ranking, since it is how every fraction in it is to be read.

`likelihood.convolutional.bcjr` is ported to Rust (`src/bcjr.rs`) behind a `backend` argument `turbo.decode_turbo` passes through, and the default moves to `Backend.RUST`: **34.8x / 35.0x** one pass and **26.2x / 26.3x** an eight-iteration turbo decode at the declared `K = 256`, saving **35.6 ms** of that decode's 37.0, and 24.7x on the stress waterfall. Agreement with the NumPy oracle, which stays, is **bitwise** at `memory` 1 and 2 — every register the fixtures declare — and 2.3e-13 absolute, 2.2e-12 relative at 3 and 4, where NumPy's pairwise sum over states reassociates and the kernel does not; the log evidence is bitwise throughout.

The Swendsen-Wang cluster pass is cut twice and then ported to Rust (`src/potts.rs`), behind a `backend` argument on `potts_mcmc._swendsen_wang_sweep`, `potts_keyed.SwendsenWangMove` and a new `cluster_backend` on `sample_potts`. The cuts are **2.04x** and bitwise --- the field multiply hoisted out of a loop that runs once per cluster, and the clusters grouped by one stable sort where each had scanned the whole labelling --- and the port is **63.4x / 63.2x** on top of them: `SwendsenWangMove.propose` at 64x64 goes from **20.200 / 20.692 ms** to **318.5 / 327.6 us**, saving 19.88 ms of the pass, and the ranking's 42.2 ms row ends at 0.32 ms. The NumPy pass stays as the oracle and remains the **default**: the kernel draws the colours and accept uniforms in bulk where the oracle draws them lazily, so it returns a chain of the same law rather than the same chain. Given the same draws it is the oracle bitwise, guarded per cluster as the single-site sweep is per site, and the law is pinned against the enumerated 2x2 distribution at two seeds with and without a field.

The `message_passing` tree schedule's two passes are ported to Rust (`src/message_passing.rs`) behind a `backend` argument on `sum_product` and `max_product`, and the default moves to `Backend.RUST`: **9.05x / 9.28x** a `sum_product` at the chain of 200 the stress profile ranks, saving **21.6 ms** of that call's 24.3, and 10.1x at a chain of 2,000. The algorithmic cut beside it --- the per-level plan stored per graph rather than rebuilt per call --- is measured at **1.23x** and declined, since the kernel builds no plan at all. Which schedules the kernel runs is the schedule's own `compiled`, so no consumer branches on a name. The NumPy route stays as the oracle: the assignment, the guarantee and the pass count are equal, and the marginals and `log Z` agree to 3.3e-15 and 1.4e-14 absolute, inside `CROSS_DEVICE_RTOL_FLOAT64`, where NumPy's vectorized `exp` and `log` differ from `libm`'s in the last place.

The Potts chain's normalizer reassociates its homogeneous transfer product by repeated squaring (`opt.potts.log_partition_by_squaring`), taking `T ** 63` in five squarings and six vector-matrix products where the per-site recursion took 63 in sequence --- 12 `torch.logsumexp` calls per evaluation against 64, in the forward pass and in the tape autograd walks back. `hmc.sample` at 1,000 draws on the chain of 64 goes from **23.501 / 23.597 s** to **6.601 / 6.511 s**, **3.57x**, saving 17.0 s of 23.5, and the ranking's 41.7% `run_backward` row falls from 10.19 s to 2.93 s with it: it was this loop's tape and not a term outside the package. `log_partition` picks the route on `q` and the length together, since squaring pays a cube in `q` to buy a logarithm in the length --- 4.51x at `q = 3, length = 64` against 0.04x at `q = 64, length = 4` --- and takes it only where it makes no more calls and touches no more elements than the recursion, which leaves the `ci` fixture's chain of 12 on the recursion. That recursion stays as the oracle (`log_partition_by_recursion`): agreement is **bitwise** at lengths 1 and 2, where both run the same sequence, and within `CROSS_DEVICE_RTOL_FLOAT64` elsewhere, realized 6.815e-16 relative at the stress instance and 6.13e-14 at worst over the sizes the route is taken at.
