`opt.split_merge.split_and_merge`: after EM converges, merge pairs ranked by responsibility cosine and split candidates by local Kullback-Leibler divergence are tried in turn, partial then full EM, each accepted only if the log-likelihood rises. The starts notebook offers `EM + split-merge` beside plain EM at equal budget.
