The pruning routes share one post-order, leaf indicator, rescaling step and set of validations (`likelihood.pruning_common`), the two iterated-conditional-modes sweeps share one body, and `alpha_expansion`/`alpha_beta_swap` share one expansion template; every folded call returns what it returned before, bitwise, and `likelihood.pruning` stays the oracle with no shared code in it.
