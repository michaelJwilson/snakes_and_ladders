The textbook states what the count-pair instance and the ragged chains are
(issue #681). Three catalogue keys --- `spatio_sequential_counts`,
`spatio_sequential_ragged` and `ragged_hmm` --- resolved to a section whose
five parts were complete while the model each declares was stated nowhere, and
`test_problem_statements_complete.py` could not see it, because it checks one
statement per section. `sec:hmm` and `sec:coupled` now carry the likelihood of
chains of unequal length, with padding named as an implementation of it and
never the model; `sec:coupled` carries the per-channel covariate contract ---
an exposure multiplies the rate, a trial count replaces the declared one ---
with the recovery that makes it a conditioning rather than a
reparameterization. The guard now asserts a sub-part for every catalogue key
that shares a section with another, read from `PROBLEMS.md` so a key added
there cannot drift away from the statement that has to state it.
