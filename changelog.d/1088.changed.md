The per-PR critical gate runs once, under coverage, instead of bare and then instrumented (#1088): 103 s against 65 s + 103 s on the local replica. Sixteen tier tests over the 10 s cap `DEV.md` states carry `release`, 385 s of the tier's 1,555 s of test time (25%), measured in the #1088 profile at four workers; the two `critical` tests over the cap stay in the tier.
