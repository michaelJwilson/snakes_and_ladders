Five documentation claims the 0.5.0 audit (#468) found untrue, corrected where each lives. `DEV.md` and `changelog.d/425.removed.md` both said the #422 reachability walk saved nothing; #445 measured the case the twelve-branch sample never contained --- a change reaching only some figures stales 19 of 19 cited figures under the closure digest against the walk's 1, with no figure's bytes moving --- so both copies now record #425's removal as a decision taken on an unrepresentative sample rather than as a measurement that held. `python/snakes_and_ladders/sandbox/CLAUDE.md` said "Nothing yet" while the directory shipped `tropical.py`, and its header admitted only the case where a framework replaces a hot path, not the declined route that `tropical` is; both now say what is there. `.github/ISSUE_TEMPLATE/release.yml` listed nine problem-statement boxes against the textbook's eleven sections, so a release audit could tick every box without reading two statements; the count-emission mixture and turbo decoding are added. `ROADMAP.md` and `README.md` did not name the turbo code, which landed as a problem class in #233 --- the roadmap now names it as the codes class's second family in §1.1 and in Milestone 1.1's deliverable, and the README's four-recursion sentence says its BCJR pass is sum-product on a trellis.
