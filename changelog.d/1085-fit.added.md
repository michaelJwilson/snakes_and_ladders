`opt.split_merge.fit(observations, n_components, at, *, method, rng, restarts=1, refine=False)` seeds, fits by EM, refines by split-and-merge and restarts in one call, from the observations alone, keeping the fit of highest log-likelihood; each restart draws from its own spawned stream (#1085).
