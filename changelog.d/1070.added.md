`search.alpha_expansion.fuse(graph, field, first, second)`, the fusion move (Lempitsky et al. 2010): per site, one proposal's label or the other's, chosen by the roof dual's cut over two nodes per differing site, and never worse than either proposal. On `spatio_tiling/release` it takes 3.4 ms on the Rust cut and 105 ms on the Python one; fusing alpha-expansion's labelling (-17,021.987) with an anneal's reaches -17,022.199 on two of five seeds, below both inputs, and leaves at most 18 of 424 differing sites to the better proposal. `search.maxflow.max_flow` takes `backend=` (default `PYTHON`, unchanged), the gateway `fuse` reaches the Rust cut through.
