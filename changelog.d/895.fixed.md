`main` is green again on push for three tests that had failed on every push run since they landed in #771 and #776 (2026-09-19), with bit-identical values at every merge: no merge moved them, the host did. Both computations are seeded and their last bits follow the code path MKL picks for the CPU; `MKL_CBWR=COMPATIBLE` reproduces the GitHub runner's values bitwise. The tropical-relaxation residual is float64 rounding, two ulps of the score on the reference host and one on the runner, and is now pinned at the corner tolerance `test_sandbox_tropical.py` declares for the same comparison (1e-14 of the score) rather than to its bits. The HMC test's marginal oracle passed on the runner and is unchanged; its energy-error claim asserted one chain's largest energy error, which exceeds its bound of 100 on one seed in five on two of three MKL paths, and now holds as a median over five seeds fixed in advance, with the bound and the 1,000x separation unchanged.
