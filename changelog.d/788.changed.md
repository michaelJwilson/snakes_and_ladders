The 2026-09-19 audit (`docs/reviews/2026-09-19-audit.md`) reads the
repository against what it declares: 206 `CLAUDE.md` rules (60 enforced by
no test), 13 milestones against their roadmap validation sentence (7 meet
it), 14 declared seams (3 pinned), 294 textbook labels (33 cited from a
test). Fixed with it: root `CLAUDE.md`'s #289 precedent and host rule, the
budget's unit in `search/` and `learn/`, the Expected Reader contract in
`docs/`, four rules stated twice trimmed to their owner, two module files'
measurements, `ROADMAP.md`'s tier counts (534 / 3,402 / 3,527), and the
label guard reading `par:` and `tab:`.
