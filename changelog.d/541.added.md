Compared eight seedings of each state's emission parameters on the key spatio-sequential model in projection, budget-matched through `opt.budget.compare`; `snakes_and_ladders.search.projection` carries the projection and the candidates, and `opt.initialize` three chain-based starts.
