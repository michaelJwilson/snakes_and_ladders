`sal.sim` and `sal.opt` draw their fixtures through a Rust categorical sampler, `sal.numerics_rust.sample_rows`, backed by `sal.oxi_snakes_and_ladders`. Issue #181's audit measured the NumPy original at 94-96% of `simulate_alignment`'s self time; the port is 2.6x faster at 200,000 draws and 1.7x at 2,000,000, and bit-identical to it — the uniforms are still drawn by the caller's `numpy.random.Generator`, so a seeded simulation produces exactly the alignment it did before. `sal.numerics.sample_rows` stays as the oracle the port is pinned against.
