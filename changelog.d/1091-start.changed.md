The initial point is `start` wherever it was `theta0`: `fit`, the HMC `sample`, `anneal` and `parallel_tempering`, `random_walk`, `replay`, `mala`, `slice_sample`, `run_chain`, `run_compiled` and `start_point`. The ladder adapters (`adapt_ladder`, `adapt_ladder_by_round_trips`, `adapt_ladder_round_trips`, `adapt_ladder_potts`) take their starting ladder as `temperatures`, as the samplers that run a ladder do. An old keyword raises `TypeError` (#1091).
