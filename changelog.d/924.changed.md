An EM step forms the joint log-density once for both the log-likelihood and the responsibilities (`opt.mixture.e_step`), and the count families take the `lgamma` of a count plus a per-state parameter on the distinct counts only: the E step on `emission_mixture/stress` from 5.2 ms to 2.1 ms an iteration, bitwise.
