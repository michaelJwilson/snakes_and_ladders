`docs/nb/potts_starts.ipynb` runs on `spatio_only/release` (71x71 triangular, q = 10) in place of `potts_lattice/release` at q = 3 and its q = 2 sibling, as one `qa.starts.gap_panels` panel read against the alpha-expansion bracket, at 300 sweeps a solver. `search.potts_starts.spatio_rung` builds the rung from the declared fixture. Alpha-expansion and alpha-beta swap reach -10,454.16 on every seed, the fixture's graph-cut ground state at q = 2; the next solvers end 293.7 (max-product) and 295.3 ± 71.1 (annealing) above it.
