`docs/source/index.rst` was missing five modules — `sal.sim.graph`, `sal.sim.potts`, `sal.sim.hmm`, `sal.scripts` and `sal.scripts.run_snakes_and_ladders` — the third drift of an invariant `docs/CLAUDE.md` states and that `sphinx-build -W` structurally cannot catch, since it fails on a broken entry and never on an absent one. All five are added, and a millisecond test now enforces the invariant in both directions.
