`opt.emission_mixture.plus_plus_start` now scores candidates by the family's Bregman divergence, the quantity `Emission_Mixture++` is defined over, rather than by its negative log density. The log normalizer the density carries does not cancel under D-squared sampling, which normalizes its scores rather than shifting them.
