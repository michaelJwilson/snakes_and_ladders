`docs/nb/potts_starts.ipynb`: every ground-state solver of `search.ground_state` on the declared `potts_lattice/release` instance (50x50, q = 3, critical coupling, size-tilted field) and its q = 2 sibling, each polished by iterated conditional modes through the `opt.starts` seam at five seeds, read against the alpha-expansion bracket at q = 3, the exact graph-cut optimum at q = 2 and the structure of the generating ladder. New `search.potts_starts` (`PottsObjective`, `SolverStart`, `polish_by_icm`, `rung_of`, `binary_sibling`, `describe`), the adapter onto the seam; `qa.starts.gap_table`, the five-column table of a `SolverComparison`; a `potts_lattice` fixture may declare its field as a size tilt (`alpha`, `size_spread`, `size_seed`).
