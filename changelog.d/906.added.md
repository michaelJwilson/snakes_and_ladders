`docs/nb/potts_starts.ipynb`: every ground-state solver of `search.ground_state` on the declared `potts_lattice/release` instance (50x50, q = 3, critical coupling, size-tilted field), polished by iterated conditional modes, read as an energy gap against the expansion bracket and the structure referee on a log runtime axis through the `opt.starts` seam, generalized over a labelling state; a q = 2 sibling where the graph cut is exact.
