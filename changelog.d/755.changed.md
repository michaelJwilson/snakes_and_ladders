`sal.enumeration.enumerated_optimum` is the one enumerate-score-argmax composition: `learn.potts.optimum`, `learn.hmm.optimum` and `learn.relaxed.enumerate_optimum` each wrote it in full and now call it, returning bitwise what they returned before. `likelihood.hmm_paths` keeps its own argmax, named in `tests/regression/test_duplication_guards.py`, because it reads the score vector again for the posterior.
