`fit_spatio_sequential` takes `backend: Backend` like every other kernel choice: `likelihood.spatio_sequential`'s `class_posteriors`, `external_field` and `labelled_log_likelihood` dispatch to the tabulated Rust kernel on `Backend.RUST` and refuse any other member by name, and the `CoupledBackend` triple with `NUMPY_BACKEND` and `RUST_BACKEND` is removed. The NumPy oracle is untouched and the Rust route is bitwise the twin's own call (#828).
