The run tracker reaches every loop: `slice_sample` records its objective evaluations per draw, `annealed_importance_sampling` and `population_annealing` record `log Z`, its standard error and its effective sample size per rung, `simulated_tempering` records the rung, the acceptance, the sweep rate and the per-rung occupation, the two tempered ensembles record round trips, the up fraction and the sweep rate, and `opt.fit.fit`, `hmc.anneal`, `hmc.parallel_tempering` and both ensembles record what the run cost the machine; `mala` was already recorded through the chain it runs. Every series ends at the field its result returns, and the null run is bitwise what it was (#799).
