An experiment file under `docs/experiments/` is three sections — question,
numbers, finding — in a body of at most ten non-blank lines, the front matter
and the title excluded; `infra/experiments.py --check` fails a file over the
cap.
