hmmlearn and scikit-learn as oracles and timing references for EM (#975): the `validation-hmmlearn` and `validation-scikit-learn` extras, their adapters and scripts, run in a subprocess. `opt.hmm.baum_welch` agrees with hmmlearn's `CategoricalHMM`, and `opt.mixture.expectation_maximization` with scikit-learn's `GaussianMixture`, to 1e-11 after one and after ten iterations from one start. Goal tests hold each to its framework's runtime at 10^5 and 10^6 positions or draws, met on both, and `tests/benchmarks/test_opt_external_em_bench.py` times both pairs.
