`learn.canonical`, `learn.environment`, `learn.hmm`, `learn.potts` and `learn.potts_nd` import no torch (#1011, step 2): an environment takes no derivative, and its features are constants to every loss the learners differentiate. `Environment.features` returns a `float64` `np.ndarray` where it returned a `torch.Tensor`, in every environment including `learn.tree`'s, and `PottsEnvironment.greedy_weights` and `HmmEnvironment.greedy_weights` return arrays. The policies take either and convert once where features meet the weights (`learn.policy.FeatureRows`, `LinearPolicy.set_weights`), as does `learn.critic.state_features`, so an environment that still returns a tensor still trains. `Environment` and `PottsEnvironment` were in release 0.3.0, so for one release `learn.environment.features_tensor(environment, state, actions)` and `PottsEnvironment.greedy_weights_tensor()` return the tensors 0.3.0 returned, bitwise, with a `DeprecationWarning`; both are removed in the release after the next. Seeded learner outputs on the seven environments' fixtures (REINFORCE, PPO and actor-critic weights, rollouts, the enumerated return and its gradient, expert iteration, the arena's five rows over 81 starts; 57 records) are bitwise unchanged. Median of 5 fresh interpreters, under load: importing each of the five takes 110 to 166 ms at 26.5 to 32.9 MB peak RSS, was 1,366 to 1,558 ms at 620.5 to 622.5 MB.
