`search.spatio_sequential.fit_spatio_sequential(min_label_sites=...)`: at the start of every block, the sites of a class holding fewer than this many are relabelled uniformly from the other classes (`redraw_small_labels`); the default 0 changes nothing.
