A finite Gaussian mixture as a problem class — `sal.sim.mixture` generates, `sal.opt.mixture` fits — whose component M step is the emission family's, unchanged from the hidden Markov case. `KMeansPlusPlus` is the first initializer that reads its objective's data, and refuses an objective whose parameters it cannot interpret.
