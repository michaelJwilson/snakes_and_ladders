Four derivations written once (#863): the repository root in `infra/_paths.py` and `tests/_paths.py` (53 sites), the marker reading in `infra/test_kinds.py` (3), the `PROBLEMS.md` table in `infra/catalogue.py` (9 readers, one of which had no short-row guard), and LaTeX escaping in `infra/tex.py` (6 escapers, from fifteen characters down to one). `pytest` puts `infra/` on the path through `pythonpath` rather than 25 test modules doing it themselves; the QA scripts read their parameters through the fixture registry rather than through nine flag-bound loaders; and the suite's Potts environment, chi-square oracle and pruning refusals each have one home. Every catalogue reader sees the same rows, every generated document and figure is byte-equal, and `pruning_analytic` gains the three validation tests it lacked.
