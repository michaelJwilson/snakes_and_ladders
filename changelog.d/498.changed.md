The `search_topologies` figure renders from the 5-taxon `tree_search/ci.yaml`
fixture rather than the 6-taxon `stress.yaml`: 15 unrooted topologies instead
of 105, 32.0 s to 7.7 s. It draws two trees and never the sweep, which only
identifies the runner-up, so exhaustiveness is all it needs from the
enumeration. `search_trajectory` and `rl_reward_surface` were measured against
the same change and keep the 6-taxon fixture; `python/snakes_and_ladders/qa/manifest.py`
records why for each.
