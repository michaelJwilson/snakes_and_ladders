The Rust extension is renamed `snakes_and_ladders.oxisal`, from `snakes_and_ladders.oxi_snakes_and_ladders` (issue #1003). Code importing the extension directly changes its import; the package API is unchanged.
