JAX as a validation framework for the gradients HMC spends (#991): the `validation-jax` extra, `sal.validation.jax` and its script, run in a subprocess so autodiff in the package stays PyTorch. `hmc.gradient_at` agrees with `jax.value_and_grad` within 1e-12 on the diagonal and dense Gaussians and the three-component mixture likelihood. `scripts/package.py` gains a `gradient` call timing three torch routes. Goals hold `gradient_at` to JAX's `jit` runtime and memory where JAX is ahead; `tests/benchmarks/test_gradient_jax_bench.py` times every route.
