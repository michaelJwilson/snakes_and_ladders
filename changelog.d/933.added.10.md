`search.spatio_sequential.merge_step` lowers the number of occupied classes: every pair is merged and refitted once, and the best merge is kept where the labelled joint, less an optional per-class penalty, does not fall (R7).
