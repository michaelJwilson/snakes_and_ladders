Every Potts sampler, solver, cut and bound that took a bare per-site field now also takes a `SiteField`: the annealers, `parallel_tempering`, the ladder adapters, the pair samplers, `cluster_moves`, `simulate_potts`, `ground_state`, `ground_state_energy_bounds`, `dual_bound`, `ising_ground_state`, `cut_energy` and `from_potts`. `field_values` is renamed `field` on the last five (#1091).
