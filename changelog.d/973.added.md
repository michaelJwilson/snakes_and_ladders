PyMaxflow as the first validation framework (#973): the `validation-pymaxflow` extra, `sal.validation.pymaxflow` and its script, run in a subprocess. `tests/validation/test_pymaxflow.py` pins `search.maxflow.ising_ground_state` on both backends to PyMaxflow node for node and its energy bitwise at 10², 71² and 142², and `max_flow` and `maxflow_rust.min_cut` on directed networks to its value within 1e-12 and its source side node for node. `tests/benchmarks/test_maxflow_pymaxflow_bench.py` times the Rust kernel beside PyMaxflow's build and cut at 16², 71², 142² and 284².
