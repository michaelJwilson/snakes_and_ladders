Learned surrogates reach the `spatio_only` Potts lattices: per-site fields
flow through `lattice_features` and `lattice_tokens`, three lattice targets
name each rung's oracle, and `decoupled_ground_energy` and
`saturated_log_partition` bracket a fit at sizes the spanning-tree bound does
not reach.
