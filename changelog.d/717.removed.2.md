`sal.numerics_rust`: the compiled categorical sampler is `sal.numerics.sample_rows(backend=Backend.RUST)`, the default, bitwise the NumPy oracle's answer on the same generator state; `Backend` lives at `sal.backend`, imported from `search.backend` before (issue #717).
