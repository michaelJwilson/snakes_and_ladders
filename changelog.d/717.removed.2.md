`snakes_and_ladders.numerics_rust`: the compiled categorical sampler is `snakes_and_ladders.numerics.sample_rows(backend=Backend.RUST)`, the default, bitwise the NumPy oracle's answer on the same generator state; `Backend` lives at `snakes_and_ladders.backend`, imported from `search.backend` before (issue #717).
