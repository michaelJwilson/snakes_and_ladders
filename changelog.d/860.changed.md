The unit a method spends is one vocabulary: `Cost` moves from `infra/ladder.py` to `snakes_and_ladders.cost`, `opt.budget.Budget.unit` is a `Cost` rather than a `str` spelled eight ways, and the five units a budget named that no rung spends --- `decisions`, `fits`, `site visits`, `passes`, `candidates` --- are members. `Comparison.table()` prints what it printed for six of the eight; `"site-visits"` and `"site visits"` were one unit under two spellings and are now `site visits`, and `"evaluations"` is the ladder's `objective evaluations` (#860).

Whether a loop finished and why is one `opt.termination.Termination` --- `converged`, `iterations`, a `Stop` of three values --- carried as an additive trailing field on fourteen optimizer results that answered it five ways between them. Every existing field keeps its value; `None` is *not known*, and the rows that cannot say which branch ended their loop leave it.
