`docs/templates/work_in_flight.html` is the template of the work-in-flight page, after michaelJwilson/port#335. It has a header with counts, the merge order by stack, the open pull requests with CI state and next action, the queue, and the tickets filed. `docs/templates/README.md` states each section's source, the state chips and the refresh procedure.
