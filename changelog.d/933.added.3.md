`NegativeBinomialEmission(..., tied=True)` shares one dispersion across states and `BetaBinomialEmission(..., tied=True)` one concentration, each state keeping its rate; the M step solves the score summed over the states, pinned to `scipy.optimize` on the likelihood (R3).
