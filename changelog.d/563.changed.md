`snakes_and_ladders.search.gibbs`'s recorded log-density runs through a `numba`
kernel over the same edge layout the sweep walks, summing factors left to right
in graph order. `Backend.PYTHON` --- `FactorGraph.log_density`, unchanged --- is
the oracle it reproduces bitwise, so every chain is the chain it was.
