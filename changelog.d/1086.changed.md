Test selection is by import at file level (#1086): a change runs the test files whose import closure contains a changed module, read with `ast` over `sal`, the `tests` helpers and `infra`; `src/` counts as a change to `sal.oxisal`; tests that read the package source run on any package change. `sample` and `validation`, missing from the subpackage list since they were created, are attributed. The whole suite runs only for what no import can say (lockfiles, `pyproject.toml`, the root `conftest.py`, fixtures, workflows): 7 of the last 30 merges, against 13.
