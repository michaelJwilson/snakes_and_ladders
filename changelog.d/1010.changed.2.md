`sample.chain` holds the chain machinery `sample.hmc` carried (the kernels' shared warm-up, dual averaging, the compiled driver and `compiled_route`); `hmc` re-exports what it moved. `tempered.exchange` is public, and `opt.objective.autograd_value_and_gradient` is the one autograd fallback (#1010, C2/C4/N4/N5).
