`NegativeBinomialEmission` and `BetaBinomialEmission` take a zero exposure or a zero trial count as an unobserved channel: the entry scores log 1 and the M step drops it, bitwise the fit without it; `IndependentCountPair` masks each channel on its own. Negative, `nan` and fractional covariates are still refused (R2).
