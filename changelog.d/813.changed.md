`search.maxflow.ising_ground_state` and `sim.count_pairs.simulate_count_pairs` take `backend: Backend`, default `PYTHON`, so a caller names the kernel rather than the twin module; the oracle path is unchanged and the Rust path returns the twin's answer bitwise. `opt.potts.simulate_chains` draws through `sim.potts.simulate_potts` on an open chain, the same states bit for bit at the declared instance; the copy it carried and its private `_softmax` are gone.
