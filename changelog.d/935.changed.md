`search.alpha_expansion`: the expansion and swap networks cross to the Rust cut as arrays, with no Python list store, and the swap network is built without a call per arc: at 80,656 sites and ten labels the Rust expansion runs in 2.88 s (from 8.56) and the swap in 1.20 s (from 5.14), the same labellings.
