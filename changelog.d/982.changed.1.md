A second cut takes the per-PR tier to 3,877 tests and 82,515 lines under `tests/` excluding benchmarks (−8 and −260 on the first): the goals are two parametrised tests over `RUNTIME_GOALS` and `MEMORY_GOALS` in `tests/validation/test_goals.py`, with every figure and the 0.55 ratio unchanged; eight guards that forbid a spelling outside its owner read one `ABSENT` table; the sampler recovery assertions live in `tests/_posteriors.py`; and a duplicated exposure-recovery test is removed (#982).
