Pull requests change `docs/tex/` and never the committed PDFs, which a "Rebuild the documents" ticket's pull request rebuilds (new issue template; CI refuses a PDF change elsewhere and no longer compares the PDFs); `main`'s CI runs are no longer cancelled by the next merge; the test-selection step fetches the base branch's history; the document build runs against the synced environment instead of rebuilding the Rust extension; `infra/measure_test_budget.sh` measures the critical tier unless `--full`.
