`baum_welch_family(..., update=)` takes a `CovariateUpdate` that recomputes the covariate from the current family and the previous posterior before every E step; `ExpectedRateNormalizer` divides each sequence's exposure by `Z_c = sum_g lambda_g E[mu_{s_c(g)}]`, a normalizer that depends on the parameters (R8).
