The textbook ships as one PDF and one PDF per section, with the front matter and contents as their own: `infra/split_textbook.py` reads the book's `.toc` and `infra/build_documents.sh` cuts each section's pages out of `docs/textbook.pdf` with `pdfpages` into `docs/textbook/NN-slug.pdf`, so the source stays one file and the book is byte for byte what it was (#844).
