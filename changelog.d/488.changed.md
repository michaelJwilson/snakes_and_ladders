The paper and the textbook build on the push to `main` rather than on every pull request, and `infra/build_documents.sh` checks citation integrity before `latexmk`: a cited figure exists, a cited label is defined in the document that cites it, a `\cite` has a bibliography entry, and every bibliography entry closes its braces. The check is text and existence, 55 ms over both documents. A pull request still must not change either committed PDF; the `lint` job now carries that rule.
