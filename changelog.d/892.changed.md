The beta-binomial M step of `BetaBinomialEmission` and `CountPairEmission` solves every component at once, on each channel's distinct values weighted by the responsibility summed there (`emissions._solve_beta_binomial_batched`), bitwise the per-component solve on every draw measured. One EM iteration on the `spatio_sequential_counts/release` projection drops from 5.9 s to 0.65 s and on `emission_mixture/stress` from 0.61 s to 0.124 s, one thread. `emission_mixture_starts.ipynb` runs its five seeds, 53 cells, in 239 s.
