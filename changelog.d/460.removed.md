A baseline record no longer carries a digest of the tree it was written from. Over the whole history of the five records that field moved 54 times and carried a moved value 0 times, while making 8 of the 38 merge conflicts across the eight open branches — on all eight the three merge sides differed in the digest and in nothing else. `infra/baselines.py --changed` replaces it, recomputing the records whose fixture or module closure a change touches, on the pull request that makes the change; `snakes_and_ladders.sim.fixtures.baseline` still raises `StaleBaselineError` where the installed `numpy`, `scipy` or `torch` is not the one the record was computed against.
