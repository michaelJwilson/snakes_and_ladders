Cluster-move schedules to convergence (#1046): `qa.potts_schedule` gains a Nelder-Mead search that stops when the simplex's objective range is below one standard error on the tuning seeds and its vertices are within 1% per coordinate (`nelder_mead`, mirroring at the bounds rather than clipping), a step-count match that brackets from either side (`match_steps`), and rounds that fix a step count and a schedule together (`joint_rounds`). `anneal_potts` and `run_annealed` take Niedermayer's threshold `E_0`, defaulting to the previous run bitwise. `qa.potts_converge` searches Swendsen-Wang's, Wolff's, ghost-spin's, label-directed's and Niedermayer's schedules into `docs/nb/data/potts_converge.json`, scoring Niedermayer's threshold at its own matched count, and `potts_starts.ipynb` reports them on seeds 0 to 4. Three searches converged with no difference against their previous schedules; Wolff's and Niedermayer's were stopped at 55 and 50 evaluations, and their best schedules spend 1.91 and 2.31 times the budget.
