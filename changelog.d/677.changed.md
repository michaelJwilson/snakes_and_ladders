`infra/appraise_structures.py` sees a third spelling of one relation and stops
dropping findings. Lengths beside one flat payload --- `ragged.Ragged`, the
largest addition since the survey was written --- is a compressed layout with
the offsets implicit, so it joins `role:incidence` at 14 members over 84
consuming references against 12 over 81, bringing `sim.hmm.SimulatedHmmDataset`
with it; a payload partner is required, so a params declaring `lengths`
stays a declaration, and `sizes` is refused as a length field. A cost finding
on a class that belongs to no cluster is now printed rather than derived and
discarded: nine were invisible. `Ragged`'s per-call offsets scan is measured at
39.3 us against 335.11 ms for the iteration that encloses it, 0.012%, and kept.
The covariate contract the five slicing seams each restated is stated once in
`sal.emissions`, where it is enforced.
