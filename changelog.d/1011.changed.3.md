`import sal.sim.spatio_sequential` no longer loads torch (#1011): 29.0 MB peak RSS, was 622.7 MB. The module draws with NumPy and takes no derivative, so it imports `emissions` and torch only inside the calls that build an `EmissionFamily` or hand one a tensor (`from_declared`, `canonical_spatio_sequential`, the shared-emissions check, the covariate helpers and `gated_log_density`). No signature or stream changes. `sim.count_pairs` keeps torch at import: `IndependentCountPair` and `ReflectedEmission` implement the tensor `EmissionFamily` API at module scope.
