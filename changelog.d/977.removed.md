`snakes_and_ladders.learn.gym.GymnasiumEnvironment` and the `frameworks` extra (#977): the Gymnasium adapter had no consumer but its tests and moves into `validation/scripts/gymnasium.py`, run in a subprocess. Gymnasium, TorchRL and PyTorch Geometric leave the `frameworks` extra, which is deleted, for `validation-gymnasium`, `validation-torchrl` and `validation-torch-geometric`. Six adapter argument-check tests are dropped with the class.
