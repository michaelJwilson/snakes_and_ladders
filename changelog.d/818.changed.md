Every rung of the oracle ladder declares the unit it spends: `infra.ladder.Rung` gains a keyword-only `cost: Cost` over eight units (exact, one pass, iterations, sweeps, objective evaluations, gradients, trained, several), all 104 rungs state one, and `tab:ladder` prints it as a sixth column, `Spends`; the table names the unit and does not rank by it (#818).
