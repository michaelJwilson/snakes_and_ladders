`emission_mixture_starts.ipynb` runs each (start, seed) cell under one `Budget(Cost.SECONDS, 120)` covering the start and its EM polish, which stops at a 1e-6 relative change in the log-likelihood (`search.mixture_starts.POLISH_TOLERANCE`) or before it would pass the budget; it closes on one figure of gap [nats] against runtime [s] on a log axis, mean over seeds with a ±1 std band (`search.mixture_starts.gap_band`), and renders the five-column table of `qa.starts_table.build_table` (initializer, init. gap, init time, final gap, final time) through `IPython.display.Math` (`build_array`). The reached-gap bar panel is removed. The notebook runs one seed of the five named: 53 cells do not fit the 600 s notebook budget (#892). **Default change:** `sample.initialize.FromChain` warms up by default, `adaptation=CHAIN_ADAPTATION` (300 proposals to acceptance 0.65, step jitter 0.4); `adaptation=None` is the previous chain bitwise.
