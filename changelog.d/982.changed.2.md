A third cut takes the per-PR tier to 3,303 tests, down from 3,898 (−595, −15.3%), at 83,154 lines under `tests/` excluding benchmarks, up from 82,713 (+441, +0.5%): 165 tests that parametrised a seed or scalar grid case by case now loop over the same table in one body through `tests/_rows.py`, which runs every row and fails once naming each failing row, at the same sizes, seeds, tolerances and referee; channel families, backends, moves, routes, fixture tiers and the sandbox's non-seed axes stay parametrised, and six grids stay split because, folded, each ran 7.0 s to 20.8 s under load against the 10 s per-PR cap (#982).
