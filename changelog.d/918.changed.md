The negative-binomial M step solves every state's dispersion in one lockstep bisection on the distinct counts (`emissions._solve_dispersion_batched`), as #892 did for the beta-binomial: 13.1x on the dispersion solve and 2.3x on the joint count-pair M step at the `spatio_sequential_counts/release` projection's size, one thread. The per-state `_solve_dispersion` stays as the oracle and the exposure path; fitted dispersions agree bitwise on `emission_mixture/ci` and within 5.9e-13 relative on `/stress`.
