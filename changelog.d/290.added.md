A factor graph (`sim.factor_graph`) with adapters from a tree, a Potts graph, a hidden Markov chain and the coupled spatio-sequential model, its Forney normal form, and one sum-product / max-product (`likelihood.message_passing`) pinned to pruning, the path enumeration and belief propagation.
