The ten module `CLAUDE.md` files read against root and against each other (issue #803). The pointer paragraph every one of them carries had drifted into six wordings and is now one, and three lines shorter: it references root's **Writing Style** and states nothing root already states, where two variants had restated rule 6's own sentence. `search/CLAUDE.md` said a budget is counted "in evaluations and seconds" where `learn/CLAUDE.md` says "in decisions, never in seconds" --- the same rule stated two ways, one of them the opposite of the other; it now says what both it and `opt/CLAUDE.md` mean, that the unit is the work a method spends and the seconds are reported beside it. `sample/CLAUDE.md`'s ladder rule carried four measurements `STATUS.md` already owns and now carries the principle alone. `docs/CLAUDE.md` pointed at an **Expected Reader** section root does not have. Three of the guards over these files could not fail: the restatement check searched for a sentence root no longer words that way and now reads rule 5 from root itself; the measurement check missed comma-grouped numbers and now catches them; the **Expected Reader** check asserted the absence of a reference to something absent and now checks the direction that can fail --- a module file naming a root section root does not carry --- with a companion pinning that it does fail on one. `python/snakes_and_ladders/sample`, added by #777, was missing from the list of directories a `CLAUDE.md` is required in.
