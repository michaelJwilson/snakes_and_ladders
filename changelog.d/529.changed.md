The textbook's eleven problem-statement sketches now draw from one visual
vocabulary, defined once in the LaTeX preamble rather than eleven times, and
five supported-size paragraphs are corrected against the fixtures the suite
runs: the per-site-field Potts instance at 5,041 nodes, the count-pair coupled
instances at 1,000 and 20,000 sequential positions, the $3 \times 4$
triangular lattice and the planted glass's node range, the length the LDPC
codeword enumeration reaches, and the dimensions each test function is fitted
at.
