The textbook's problem statements and `PROBLEMS.md`'s rows are joined on one problem key and held to each other (#495). `PROBLEMS.md` gains a `Statement` column carrying the `\label` of the textbook section that states the model each row is code for; `infra/problem_join.py --check` fails a statement no row is keyed to and a row keyed to no statement, and `--report` prints the join. The key travels one way only — the registry names the document, the document names no code — so the split the two documents exist for (#249) is preserved. The join's first run found eleven statements against fifteen rows, four of them variants of a statement another row already carries, and two orphans of the same shape it reports without gating: `sim_problem_sizes` and `topology_accuracy` are rendered and cited by nothing (#492), and the release checklist has no box for the Count-Emission Mixture or Turbo Decoding.
