`baum_welch_family` takes a transition kernel per step of each sequence, `(n_sequences, length - 1, m, m)`, held fixed as a per-step kernel is; `sim.count_pairs.ReflectedEmission` doubles a beta-binomial or `IndependentCountPair` family into `2K` states, `k + K` emitting at rate `1 - p_k` from the same parameters, with an M step on the data and its reflection (R9).
