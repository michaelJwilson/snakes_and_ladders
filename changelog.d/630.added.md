A session refuses to start where the compiled `oxisal` in the
tree is older than the Rust it was built from, naming both files and
`infra/build_extension.sh`, which rebuilds it for that worktree. A stale
extension previously ran new tests against old Rust and reported the failures
as the change under test (issue #630).
