`snakes_and_ladders.validation.gymnasium`, `validation.torchrl` and `validation.torch_geometric` (#977): Farama's `check_env` and the round-tripped episode, TorchRL's `GAE`, `ClipPPOLoss` and `ReinforceLoss`, and PyG's `GINConv` twin, each run in a subprocess and pinned in `tests/validation/`, with a benchmark per row at stress sizes in `tests/benchmarks/test_frameworks_validation_bench.py`.
