Six modules moved to the directory whose sentence owns them, every import repointed and no body changed: `search.topology` is `sim.topology` (a topology and its neighbourhoods are structure, not inference) and carries `Model` and `MoveSet` from `search.infer`; `search.kernels` is `sample.kernels`; the three sampler-built initializers are `sample.initialize`; the Potts chain's `PottsParams`, `load_potts_params` and `simulate_chains` are `sim.potts_chain`; the Reed--Solomon and Hamming decoders are `likelihood.algebraic`. `tests/regression/test_directory_imports.py` declares every directory edge with the sentence that admits it and fails on a new one; the one cycle left, `sample -> search` through `score_topology`, is declared with why (#830). `forward_log_likelihood_from_density` stays in `opt.hmm`: the move needs `opt.hmm` to import `likelihood`, which `opt/`'s own guard forbids, and the root cause is the `Objective` protocol living in `opt` (#781).
