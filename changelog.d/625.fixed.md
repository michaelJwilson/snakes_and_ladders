The textbook builds again: a method note wrote `H H^T = 0` in LaTeX text mode,
so `pdflatex` stopped at the generated applicability table and no PDF was
produced. The note now sets the condition in math mode, in the repository's own
notation.
