The paper no longer references `fig:turbo-waterfall`, a figure the textbook defines. The two documents build separately, so the reference resolved to nothing and the paper typeset `??`; it now names the companion textbook in prose, as the paper does wherever it points at a formulation. The citation check added for issue #488 is what named it, at `paper.tex:207`.
