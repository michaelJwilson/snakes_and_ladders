`ROADMAP.md` and `STATUS.md` read against the tree and against each other (issue #801). **Milestone 2.3 has a `STATUS.md` section**, the second milestone found tracked only by a summary row: the comparison machinery it rests on --- `opt.budget.compare` on one budget over shared seeds, `opt.budget.mcnemar`'s exact paired test, six recorded comparisons --- is stated beside what the milestone actually asks for and does not have, an RL agent benchmarked past the size enumeration reaches, which is recorded not started on the file's own terms. **Milestones 2.0 and 3.1 declare a validation sentence**, which they did not: 2.0 says that a method named and nowhere applied is recorded as such, and 3.1 says the surrogate's cost ratio against the exact evaluation is measured at the size the claim is made for --- the `10,000x` in its text is the target it was written against and is not a result. Thirty-two `STATUS.md` references named a module the `sample/` move relocated and now name it where it lives; no number moved with them. **Stage 5 stops being a list of headings:** its three bare bullets --- attention over Newick strings, the HMM seeding and fit comparison, neural belief propagation --- each declare what referees them and the size the claim is read at, and the stage points at `docs/blue_sky.md` for the candidate list. Two candidates are added there with their oracles: sequential Monte Carlo over topologies, refereed by the enumerated flat-prior weight at 5 to 8 taxa and estimating a marginal likelihood nothing in the tree estimates for trees; and neural transport for the continuous posterior, refereed by the analytic Gaussian and the enumerated mixture posterior and measured in effective samples per gradient against HMC, MALA and slice sampling.
