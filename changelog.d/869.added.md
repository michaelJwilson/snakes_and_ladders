A textbook section, "Graphical Models, Likelihood and the Partition Function",
stating once the foundations every problem section assumes: the directed and
undirected factorizations and the factor graph both become, the partition
function and its gradient, maximum likelihood and the score identity, the
evidence as a ratio of partition functions, expectation--maximization, the
evidence lower bound, and one table of what `Z` is per problem, whether it is
exact, and the validation rung that referees it.
