`PottsGraph` derives its array form once: `edge_index` as `(n_edges, 2)` int64
and `edge_coupling` as float64, both `cached_property` and both unwritable.
Fifteen call sites across seven modules converted `graph.edges` and
`graph.coupling` per call, which was **81%** of a 200-step `anneal_potts` at
32x32; that run now takes **27.6 ms against 159.9 ms, 5.79x**, and
`alpha_expansion` 1.24-1.29x (issue #623).
