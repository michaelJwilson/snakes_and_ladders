The release gate's `sphinx-build -W` is a full build (`-E -a`) over all 134 modules, 32.2 s cold against 8.2 s incremental. An incremental build re-reads only what changed, so a warning in an untouched docstring was one it never emitted.
