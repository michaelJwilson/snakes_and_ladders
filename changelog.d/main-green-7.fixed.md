The guard against a second compressed-sparse store (`test_no_module_builds_a_scipy_sparse_store`, #764) reads the package and the notebooks and no longer the tests: `tests/regression/test_incidence.py` (#776) holds `scipy.sparse.csr_matrix` as the independent referee of `SparseIncidence`, and a referee is not a second store. `main` at 929b998 was red on it; the guard file reads 17 passed.
