Three fusion arms in `search.ground_state.ARMS` (`Fusion`): `fusion-anneal`, `fusion-sw` and `fusion-chain` run alpha-expansion on ten cycles and fuse into it one anneal, one tuned Swendsen-Wang, or four anneals in turn, each on an equal share of the remaining site visits. On `spatio_tiling/release` at 1,000 sweeps, five seeds, `fusion-anneal` reaches -17,022.199 on four against the expansion's -17,021.987 and the anneal's best -17,022.107; `fusion-chain` on three; `fusion-sw` on none.
