`docs/nb/spatio_sequential_starts.ipynb`, a seventh notebook: thirteen starts of the coupled model's emission parameters run from their own seams at one six-pass `Budget`, each fit tracked through `snakes_and_ladders.track` and closed by an `opt.budget.Comparison` against the generating parameters' own value, beside `Emission_Mixture++` on the enumerable coupled instance the 65,536-state enumeration referees. `search.projection.fit_projection` and `SeededFit` take a seeding made elsewhere, `seed_at_means` exposes the step from locations to the seam, and both fit loops record their log-likelihood per iteration under `track`.
