`docs/tex/notation.tex` defines `\Prob` and `\indicator`, which the textbook
used and no file declared; `\indicator` is `\mathbf{1}` rather than
`\mathbb{1}`, which has no glyph in `msbm` and rendered as a broken symbol.
The Hamiltonian becomes script `\mathcal{H}`, so it no longer shares a letter
with the bold parity-check matrix and the italic Hadamard matrix.
`infra/problem_join.py` keys a problem statement on the structure of the
problems part rather than on a "Problem Statement:" prefix in the section
heading, which the restructure removed.
