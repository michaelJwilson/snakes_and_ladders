The oracle ladder is declared once in `infra/ladder.py` --- per problem, each rung's callable, the rung below it and the test pinning the pair --- generated into the textbook beside the applicability tables as `tab:ladder`, and guarded: 89 rungs, 67 pinned by a collected `oracle` test and 22 carrying issue #734 rather than a blank.
The Potts ladder is pinned to its foot: parallel tempering against annealing, the dual bound and the transfer matrix against sum-product, the Goemans-Williamson rounding against alpha-expansion where the gauge admits both, and `search.ground_state`'s three runners against the kernels they wrap --- 72 rungs pinned, 17 carrying the ticket.
The tree ladder is pinned with it: the projected draw against the closed-form mixture of the flattened families, neighbor joining against the least-squares optimum over every enumerated topology, the Fitch score against the pruning log-likelihood's slope and ranking at short branches, and the learned tree surrogate against the plug-in bound's ranking --- 76 rungs pinned, 13 carrying the ticket.
The HMM ladder is pinned with them: forward-filter backward-sample draws against the enumerated posterior over paths, the coupled Gibbs sweep against the enumerated joint law over labellings, Baum-Welch's ascent and fixed point against the re-estimation equations written out, and max-product against the mode of the coupled E step's path law --- 80 rungs pinned, 9 carrying the ticket.
The codes ladder is pinned with them: syndrome correction of the perfect Hamming and Golay codes against the enumerated nearest codeword, the Reed--Solomon algebraic decode against it, successive cancellation against min-sum on the polar code's own parity check, the CSS syndrome decoder against belief propagation on the component code over a whole coset, and the turbo posterior against BCJR on one constituent --- 85 rungs pinned, 4 carrying the ticket.
The mixture ladder is pinned with them, and no rung of any ladder now carries the ticket: the chain's draws against the posterior the enumerated assignments and their evidence integrate to, the optimal clustering cost against the minimum over every enumerated assignment, the non-Euclidean projection seedings against the D-squared law of the divergence each declares, and the three chain starts against their own runs' records and Rastrigin's closed form --- 89 rungs pinned, 0 carrying the ticket.
