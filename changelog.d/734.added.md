The oracle ladder is declared once in `infra/ladder.py` --- per problem, each rung's callable, the rung below it and the test pinning the pair --- generated into the textbook beside the applicability tables as `tab:ladder`, and guarded: 89 rungs, 67 pinned by a collected `oracle` test and 22 carrying issue #734 rather than a blank.
The Potts ladder is pinned to its foot: parallel tempering against annealing, the dual bound and the transfer matrix against sum-product, the Goemans-Williamson rounding against alpha-expansion where the gauge admits both, and `search.ground_state`'s three runners against the kernels they wrap --- 72 rungs pinned, 17 carrying the ticket.
