A fourth cut takes the lines under `tests/` excluding benchmarks from 83,470 to 76,686 (−6,784, −8.1%; −7,098, −8.5%, against the 83,784 of the #982 baseline) with the per-PR tier unchanged at 3,325 tests: module docstrings are held to 12 lines and test docstrings, helper docstrings and comment blocks to 4, narration of history and numpy `Parameters`/`Returns` sections on private helpers are cut, and prose that carries evidence (a measured tolerance, a seed's or constant's justification, a realized p-value, a referee, an issue reference) is compressed rather than removed, so 251 lines over those caps remain; with docstrings stripped, the AST of each of the 266 changed modules is identical to `main`'s (#982).
