A fourth cut takes the lines under `tests/` excluding benchmarks from 83,174 to 76,390 (−6,784, −8.2%; −7,394, −8.8%, against the 83,784 of the #982 baseline) with the per-PR tier unchanged at 3,796 tests: module docstrings are held to 12 lines and test docstrings, helper docstrings and comment blocks to 4, narration of history and numpy `Parameters`/`Returns` sections on private helpers are cut, and prose that carries evidence (a measured tolerance, a seed's or constant's justification, a realized p-value, a referee, an issue reference) is compressed rather than removed, so 251 lines over those caps remain; with docstrings stripped, the AST of each of the 266 changed modules is identical to its parent's (#982).
