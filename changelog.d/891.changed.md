`docs/nb/spatio_sequential_starts.ipynb` starts the joint count-pair emission mixture of `emission_mixture/stress` (10 components, 3,000 pairs) thirteen ways, each through `CountPairSeeding` and polished by `opt.emission_mixture.expectation_maximization` for six passes, under a 120 s initialization budget, and plots the gap against wall seconds from each start's first call. New: `search.mixture_starts` (`STARTS`, `TimedStart`, `StartRow`), `qa.starts_table` (the gap and seconds as a LaTeX tabular), `qa.forney`, `Cost.SECONDS`, a `seconds` series on every `track` sample, the `spatio_sequential_counts/release` fixture, the `host-dependent` cell tag whose text `infra/check_notebooks.py` does not compare, a 600 s per-notebook budget (`NOTEBOOK_BUDGET`), and a structure check that no code cell scrolls its output. `compare` runs pooled workers at one intra-op thread, and `fit_projection` no longer runs a discarded seventh M step.
