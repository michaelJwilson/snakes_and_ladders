`docs/nb/spatio_sequential_starts.ipynb` runs its thirteen starts on the new `spatio_sequential_counts/release` fixture (10x10 lattice, `M = K = 10`, `S = 1,000`; its projection is experiment 009's 100 components over 4,000 observations), one trial of each start through `opt.budget.compare` on four workers (`TRIALS`, raised to ten once #892 brings a fit under 5 s), and reports a labelled table with the gap and recovery as mean and range, passes and seconds. The enumerable section is dropped, and each notebook's execution is held to a 600 s budget (`NOTEBOOK_BUDGET`). New: `Cost.SECONDS`; a `seconds` series in `track`'s cost record; the `wall-clock` cell tag, whose text `infra/check_notebooks.py` does not compare; `qa.forney`, the coupled model drawn as a Forney factor graph; `search.projection.TimedFit`, `LOCATION_SEEDINGS` and `Fitted.components`. `compare` runs pooled workers at one intra-op thread, and `fit_projection` no longer runs a discarded seventh M step.
