One entry, from a start (#1052): `search.ground_state.ground_state(graph, field, method, budget, rng, *, start=None, schedule=None, steps=None)` reaches every `METHODS` entry and the new `ARMS` registry (`tuned-swendsen-wang`, `matched-wolff`, `warm-anneal`, `swendsen-wang>expansion`, `expansion>swendsen-wang`, with #1038's schedule and step count frozen as constants). ICM, the annealers, the cuts, the warm chain and the hybrids start from `start`; `greedy`, `tempering`, `max-product` and `bifurcation` refuse one by name. Solvers read a `Problem(graph, field, n_states)`, which `Rung.problem` returns; `run_annealed`'s `initial` is renamed `start`, and the two hybrids move from `search.cluster_moves` to `search.ground_state`. With no start, schedule or step count all 30 pinned cells (22 `METHODS`, 8 `ARMS`, seeds 0-4, `spatio_only` ci and release) reproduce the code before the change bitwise.
