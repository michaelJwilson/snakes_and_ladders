`sal.sandbox.count_emissions` conserves the negative-binomial
and beta-binomial families as they stood before they took a covariate (#631).
The covariate-aware families must reproduce them bit for bit where the
covariate is constant, and against a conserved implementation that is two
objects scored in one test rather than a claim about deleted code. The solvers
and the identifiability bounds are carried too, since `_solve_dispersion` takes
a tensor in the live module once the exposure lands. `sandbox/CLAUDE.md` gains
the rule this move needed: a route is conserved when it is superseded in
*capability*, not only in speed, and must still referee a case the pull request
names.
