The four annealers return one `sal.sample.runs.Annealed` each: `anneal_potts`, `anneal_factor_graph`, the HMC `anneal` and `anneal_topology`. The best point is `best` (was `labelling`, `state`, `theta`, `topology`), the per-step record `trace` (was `trajectory`), the cost `spent` in a declared `unit` (was `site_visits`, `force_evaluations`, or not reported), and each carries `final` and a `termination`. The value keeps its sign's name: `energy`, `log_density`, `value`, `log_likelihood` (#1090).
