Chains of unequal length: `sal.ragged.Ragged` carries the
segments, `opt.hmm.baum_welch_family` fits either shape through one recursion,
and `likelihood.ragged_rust` is the compiled path, which pads nothing.
