`search.maxflow.energy`, `search.maxflow.site_field` and `search.alpha_expansion.energy`: one energy and one site field remain, `sal.sim.potts.energy` (a labelling), `energies` (a block) and `site_field`, which now takes the column count a cut or an expansion expects (issue #717).
