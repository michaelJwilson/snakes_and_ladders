The duplication survey counts what issue #813's target is read against: **package lines** (51,516, non-blank, the sandbox excluded as it is from every row there), **test modules** (251) and **test lines** (56,986, over `tests/regression/test_*.py`). `tests/regression/test_duplication_guards.py` pins all three beside the rows it already holds, so a fold that lands shows as a row that falls and a row that rises has to state why. The test-lines row excludes the module that pins it: a count including its own pin moves every time the pin is edited, and the first two readings taken while writing these rows disagreed by exactly the comment that explained them.
