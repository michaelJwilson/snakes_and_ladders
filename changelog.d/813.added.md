`docs/reviews/2026-09-20-calling.md`: the calling audit, read by hand. Eighteen problems are called five ways for the same intent, six cross-directory import edges break the division each directory's `CLAUDE.md` states, and the four overlap rows hold one duplicate and three names. Every count reproduces from the command beside it.
