# Review, 2026-09-17

**TL;DR.** Read at the top of the stack, not on `main`: `dev` at `e6bac42` is
`main` (`f6217af`) plus the 19 open pull requests, 57 commits, six of them
resolutions. Every one of the 19 is green alone and the sum is not: #701's
test imports the class #709 renamed, so the stacked tree collects with **one
error**; `learn/CLAUDE.md` is 139 lines against a 120-line budget that six
pull requests each passed under; the four-document build had to be written
by hand because #578 and #665 each added "the third" document to the same
seven lists. None of that is a defect in a pull request --- it is what a gate
that runs per pull request cannot see, and #685's finding in a second form.
The science holds where it was checked: the critical tier is 384 collected and 382 passing in 66.1 s, the two failures being two of those findings on
this tree, and no published number was withdrawn. Fixed here: nine stale
statements across eight files. Filed: four tickets (§8).

Issue #679, repeated on the stack at the owner's request. The baseline is the
2026-09-16 review (#687, `main` at `c9f4250`); `main` has moved by two pull
requests since (#680, #692). Every count below was taken on `dev` at
`e6bac42` on this host on 2026-09-17 --- 4 cores, 1-minute load 0.53 at the
start of the collection runs, Python 3.12.3, `torch` 2.13.0, `numpy` 2.5.2,
`pytest` 9.1.1 --- or says why not.

## 1. What moved: the 19 pull requests the stack carries

104 pull requests merged to `main` since #376 by first-parent count; 2 since
the last review. The stack adds 19 unmerged ones, by milestone:

| Milestone | What the stack adds | Carried by |
| --- | --- | --- |
| §0 loop | Two more documents, each generated from the tree and edited by no one: the API map (1,528 entries, #578) and the mind map (#665); the last review itself (#687); the Rust survey (#694); textbook statements for the three catalogue keys that shared a section (#697); a caption that agrees with its renderer (#698) | #578, #665, #687, #694, #697, #698 |
| 1.1 | Polar codes, a twelfth statement when read and a conserved route in `sandbox/` once the owner moved it there after this review (#605); the algebraic codes and the capacity they are read against (#700) | #605, #700 |
| 1.2 | `FlowNetwork` keeps its contiguous form (#659); regions and the Kikuchi free energy (#695); the plaquette structure with two consumers (#699); the thread pool decided by concurrency shape (#621) | #621, #659, #695, #699 |
| 2.1 | The sizing harness (#701) and the Potts lattice sized with it, which renames the baseline from ICM to annealing (#704); the canonical control fixtures (#702) and tabular Q-learning and SARSA on them (#703); the N-D Potts environment with a per-site field and a keyed deterministic `step` (#707), its Wolff and Swendsen--Wang control arms (#708); one five-row table per problem, and `TreeEnvironment` (#709) | #701--#704, #707--#709 |

Milestones 1.3, 1.4, 1.5, 2.2, 2.3, 3.1 and 4.1 do not move on the stack.

## 2. Consistency audit

Each row names the two texts that disagree. **Fixed** rows are edited in this
pull request; the rest carry a ticket. Rows marked *stack* disagree only on
the integrated tree: each pull request is consistent alone.

| Where | Says | Against | Action |
| --- | --- | --- | --- |
| `tests/regression/opt/test_baseline_failure.py:31` (#701) | imports `TopologyEnvironment` | #709 renamed it `TreeEnvironment` and guards the old name | Fixed, *stack*; not pushed to #701, whose base has no `TreeEnvironment` --- a note on #709 names the merge that carries it |
| `learn/CLAUDE.md` | 139 lines | `test_claude_md_pointers.py`'s 120-line budget, which each of #702, #703, #704, #705, #706 and #709 met alone | Fixed, *stack*: three #597 rules moved under Local rules from after Relaxations, the two determinism rules folded into one, four stray blank lines removed; 120 |
| `STATUS.md` Milestone 1.1, 1.2, 1.4, 2.1 `**Modules.**` paragraphs | name the modules on `main` | #665's `test_mind_map.py` found 17 modules the stack adds claimed by no milestone; four of them --- `sim.polar`, `likelihood.polar`, `likelihood.polar_reference` (#605) and `likelihood.region_graph` (#695) --- moved to `sandbox/`, which the map counts as infrastructure, after this review was read, and 13 remain: `sim.galois`, `sim.reed_solomon`, `sim.elementary_codes`, `sim.capacity`, `search.decoding` (#700), `likelihood.ragged_rust` (#666), `search.tightening` (#699), `learn.canonical` (#702), `learn.tabular` (#703), `opt.failure` (#701), `learn.keyed`, `learn.potts_nd` (#707), `search.potts_keyed` (#708) | Fixed, *stack*: each named where its milestone records the work |
| `.github/workflows/ci.yml`, `CLAUDE.md:42`, `DEV.md:27,81,110,116`, `infra/build_documents.sh`, `infra/review_gates.sh`, `test_document_build_outputs.py` | #578: three documents, the third `api_map` | #665: three documents, the third `mind_map` | Resolved on `dev` by hand, four documents in all seven places; not in this pull request's diff |
| `ROADMAP.md:40` | `critical` 368, CI tier 2,860, suite 2,969 (`main` at #676) | 384 / 3,185 / 3,298 on `dev`; 380 / 3,185 / 3,301 after the two sandbox moves | Fixed, dated; the old reading kept beside |
| `DEV.md:47` | 48 flat regression modules, 54 benchmark modules | 49 / 58 | Fixed, dated |
| `DEV.md:212` | 303 benchmark tests (2026-09-16) | 324 collected, 337 with `release` | Fixed, dated |
| `README.md:207--209` | the documents table names the paper, the textbook and the mind map | `docs/api_map.pdf` is committed and `CLAUDE.md:42` names it | Fixed: a row |
| `docs/blue_sky.md:36` | "`ldpc.ipynb` stops at 996 bits" | `docs/nb/ldpc.ipynb` is `classical_codes.ipynb` since #700 | Fixed |
| `TICKETS.md` 2.1 | lists no #705 and no #706 | both are `planned` tickets with plans in their threads, and #707--#709 carry their first steps | Fixed: two titles |
| `sim/elementary_codes.py` (#700) | five code families, decoders pinned to exact rates and capacity | no `sec:` statement and no `PROBLEMS.md` row: "the algebraic half of the fourth problem class" is stated nowhere | #710 |
| `STATUS.md:3849--4229` | seven per-ticket sections after the audits, 4,229 lines | five and 3,853 at the last review; the Repository Map's ledger per milestone | #683, worse; a comment there |
| `README.md:106,212`, `docs/nb/README.md:3` | "one notebook per problem class" | 6 notebooks against 19 catalogue rows | #682 |
| `docs/tex/generated/problems_tables.tex` | stale on `dev` | regenerated by `infra/ledgers.sh`, not committed | Regenerated; by design uncommitted |
| `.github/workflows/ci.yml:4--7` | `pull_request: branches: [main]` | #708 targets `claude/learn-706-potts-nd` and ran no check | #685, unchanged |

Not stale, checked: `STATUS.md:7`'s `Cargo.toml` `0.3.0` matches; `README.md:187`'s
86.6% against 80.2% is experiment 003's REINFORCE row and #709's 88.9% is the
harness's, two measurements of one policy class at two budgets, both stated;
`CLAUDE.md:44` and `README.md:215` already carry `docs/reviews/`; the union
merges duplicated no line in `STATUS.md`, `TICKETS.md` or `docs/source/index.rst`
(checked by `sort | uniq -d`); `git tag -l` is empty as `RELEASE.md` records.

**Duplicated machinery.** `infra/appraise_structures.py` on the stack reports
seven layout findings, one new since #692: `search.potts_keyed.WolffMove`
(#708) walks `graph.edges` per call at `T = 0` where the move already holds the
compressed rows it built at construction. A one-function fix, made in #708.
The rest are #677's, unchanged.

## 3. Quality

Is each claim established by what refereed it, and is any of it theatre.

- **Two green pull requests that break together is the finding of this
  review.** #701 and #709 are each `clean` and 10/10; their sum does not
  collect. #685 reported that a *stacked* pull request runs no checks; this is
  the other half --- two pull requests to `main` run every check and no check
  runs on their union. The same shape appears twice more on this tree without
  a failure to show for it: `learn/CLAUDE.md` over its budget by six
  compliant additions, and two "third documents". A rule holds per pull
  request and nothing holds it per tree (#711).
- **The oracle rule held on every kernel the stack adds.** #708's cluster
  arms are `potts_mcmc`'s own sweeps, bitwise; #709's registry is bitwise the
  direct call; #700's decoders are pinned to an exact bounded-distance rate at
  1.64 standard errors worst; #605's list decoder equals maximum likelihood at
  `2^k`. Three published numbers were *corrected* by their reproductions
  rather than withdrawn --- the MLP row's undeclared learning rate, greedy's
  17.4 evaluations against a quoted 48, and #706's 0.30 µs premise measured
  at 12.4 --- and each correction is in the pull request that found it.
- **A ticket's cost estimates are the least-refereed numbers in the tree.**
  #706 was argued on 0.30 µs a candidate (12.4 measured); #705 on a 352 µs
  topology score (1,773--2,291 measured) and a `key` tier `tree_search`
  cannot declare. Both plans were approved on those figures. A plan that
  states a cost states where it was measured, or says it was not (#711 names
  this beside the gate).
- **The changelog has not been built since 0.3.0.** 234 fragments wait in
  `changelog.d/` (106 added, 83 changed, 39 fixed, 6 removed) against a
  `CHANGELOG.md` whose last section is dated 2026-09-03; `Cargo.toml` still
  reads `0.3.0` and no tag exists. The release process is documented and has
  not run in fourteen days of merging (#712).
- **Not audited:** the assertion bodies of the 3,298 tests, and the committed
  figures against a fresh render; both are release-gate work. The CI tier's
  wall-clock is not re-measured (§7).

## 4. Problem statements

Eleven statements carry the five labelled parts by the guard. `polar` was the
twelfth when this review was read (#605); the owner then moved it to `sandbox/`
beside #695's region graph, so it is a conserved route with its section kept
as `sec:tropical` is, and the release template's eleven boxes stand.
#697 gives the three shared catalogue keys the sub-parts #681 asked for, and
the guard now reads a `SHARED_KEYS` mapping so the next shared key fails a
test rather than an audit. What has no statement is #700's algebraic codes:
repetition, single parity check, Hamming, Golay and Reed--Solomon are
decoded, pinned and read against capacity, and named in no section and no
catalogue row (#710).

## 5. Frameworks: validation, extension, replacement

Installed under the `frameworks` extra, unchanged since the last review:
`gymnasium` 1.3.0, `rustworkx` 0.18.1, `torchrl` 0.13.3, `torch_geometric`
2.8.0.post1. Eleven test modules `importorskip` one (six at the last review).

| ours | framework | role | action |
| --- | --- | --- | --- |
| `learn.environment.Environment`, `learn.rollout` | `gymnasium` via `search.gym.GymnasiumEnvironment` | validation | none; #709's registry sits on the protocol, not the adapter |
| `sim.graph.PottsGraph` | `rustworkx` | validation, exact round trip | none |
| `learn.reinforce`, `learn.ppo` | `torchrl` | validation | none; #709 reproduces the published rows through the package's own calls |
| `learn.surrogate` | `torch_geometric` | validation | none |

No replacement is proposed; no measurement since 0.5.0 says a framework beats
what it fronts. One extension stands from the last review: a `torchrl`
collector over #686's environment, if built.

## 6. Blue sky

Against `docs/blue_sky.md`, re-read on the stack:

- **The mixed Potts move set (#706, step 6b)** is now the closest research
  item to attempt: the three single-move controls exist on the stack, each
  shown to be its classical run, and the mixed arm's question --- does
  selecting among moves beat the best single move --- has a referee in those
  controls that it did not have a review ago.
- **A learning environment over the coupled model's labels (#686)** stands as
  filed; nothing on the stack builds it.
- **The tree at ten taxa (#705, step 4)** moved from a plan to a measured
  cost: 2,027,025 topologies at ~2.3 ms each, and a fixture whose hardness
  came from a 72-candidate sweep rather than from size. It is not blocked on
  an oracle --- enumeration holds --- but on a fixture-format decision and
  hours of compute, both stated on the ticket.
- **Wishes, no referee, not filed:** unchanged from the last review.

## 7. Counts re-measured

| Count | Document said | Today, on `dev` | Where |
| --- | --- | --- | --- |
| `-m critical` | 368 | 384 collected, 382 passed, 66.1 s at load 0.19 (the two failures are §2's stack rows, fixed here; 384 passed after; 380 once #605 and #695 moved to `sandbox/`, the statement and marker guards' four `polar` parametrizations) | `pytest -m critical --benchmark-disable` |
| CI tier `not release and not stress` | 2,860 | 3,185 collected, 1 error | collection |
| deciding tier `... and not key` | --- | 3,183 | collection |
| `release` / `stress` / `key` / all | 78 / 31 / --- / 2,969 | 82 / 31 / 2 / 3,298 | collection |
| benchmarks | 303 | 324 (337 with `release`) | `tests/benchmarks` |
| regression flat modules / directories | 48 / 8 | 49 / 8 | `ls` |
| benchmark flat modules | 54 | 58 | `ls` |
| package modules / Rust modules | 151 / 9 (4,048 lines) | 168 / 9 (4,048) | `find` |
| notebooks / experiments / fixture problems / figures / PDFs | 6 / 20 / 20 / 21 / 2 | 6 / 19 / 21 / 21 / 4 | `ls` |
| `STATUS.md` lines / changelog fragments | 3,853 / --- | 4,229 / 234 | `wc` |
| merged PRs since #376 / since #687 | 224 / --- | 104 / 2 | `git log --first-parent` |
| document guards | 126 passed | 141 collected, 3 failed before the fixes in §2 (all three *stack*), 141 passed after | see §3 |

**The CI tier's duration is not re-measured, and this time the reason is a
rule.** `DEV.md:205` records 1,226 s over 2,315 tests (2026-09-10) and the last
review an upper bound of 1,487 s over 2,860 under contention. The tier is now
3,185 tests, and the deciding tier took **1,464 s (24:23)** over 2,567 tests on
#709's branch this session, so a run here would exceed the owner's 20-minute
ceiling on local jobs and was not started (#713 asks CI to report it).

**The experiment count is 19, not the 20 the last review printed:** that
review counted `README.md` beside the numbered files. 224 merged since #376
was counted from a different date; 104 is by first-parent merge commits since
2026-09-08 and is the figure to carry.

**Not measured:** GitHub's issue list --- every listing call was refused on
2026-09-17 (rate limit), as on 2026-09-16; ticket states are read from
`TICKETS.md`, the pull requests and `git log`.

## 8. Follow-up tickets

- #710 --- a statement and a catalogue row for the algebraic codes
- #711 --- a gate over the integrated tree: two green pull requests that break together, and a rule budget six pull requests overflow
- #712 --- build the changelog and cut a release: 234 fragments since 0.3.0
- #713 --- the CI tier's duration reported by CI, where the 20-minute local ceiling cannot
- #683 --- `STATUS.md` by accretion: seven sections now, a comment carrying the count
- #682, #684, #685, #688 --- open from the last review, unchanged on the stack
