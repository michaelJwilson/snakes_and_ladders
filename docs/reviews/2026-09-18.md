# Review, 2026-09-18

**TL;DR.** The coverage number now means something and the ticket shapes are
forms, but the planning documents drifted again in two days: **14
disagreements** between `ROADMAP.md`, `STATUS.md`, `TICKETS.md` and the tree,
nine of them new since the 2026-09-16 review (fixed in this pull request),
three roadmap promises nothing tickets (#739, #740, and the problems the
roadmap does not name, fixed here), six tickets the last review filed that
`TICKETS.md` never listed (added), the tier counts in `ROADMAP.md` already
24, 202 and 190 tests low (re-measured), and 13 applicability cells still
*untested* where 12 are refereed elsewhere or blocked (#684 narrowed). The
opportunities adjacent to the catalogue: **12 problems and 12 algorithms**,
each with an oracle the tree already carries, and six blue-sky candidates,
two with no referee.

Issue #737, filed from the review template #736 added. The baseline is the
2026-09-16 review (#679) and the tree read is `main` at `266cb65` (#736
merged). Every count below was taken on that tree on this host on 2026-09-18
--- 4 cores, Python 3.12 --- or says why not.

## 1. What moved since 2026-09-16

32 pull requests merged to `main` between #679's tree (`c9f4250`) and
`266cb65`, by first-parent merge count.

| Milestone | What moved | Carried by |
| --- | --- | --- |
| §0 loop | Coverage counts a test only where `end2end` or `oracle` judged it: kinds `oracle`, `end2end`, `analytic`, `smoke`, `infra`, findings `patch`, `backend`, `bug`, `warning`, `snapshot`; `structural` (853) and `edge_case` (412) retired; a judged guard at 82.6 (`search` 86.1) and its complement at 89.2 (`search` 88.6), read from one run's contexts; the public callables no judging test enters listed per package; seven issue templates for the shapes tickets restate, with a guard over the directory; the slimming survey pinned and a root that resolves every subpackage lazily; the previous review (#687), the mind map (#665), the typeset API map (#578) and the thread-pool decision (#621), the structure survey's third spelling (#692) and one red-`main` fix (#680) | #578, #621, #665, #680, #687, #692, #721, #727, #731, #733, #736 |
| 1.1 | The algebraic codes and Reed--Solomon with the capacity referee (#700); polar codes conserved in `sandbox/` (#605); the compiled kernels derived and the one that misses its bar named (#694) | #605, #694, #700 |
| 1.2 | Regions and the Kikuchi free energy, conserved (#695); `FlowNetwork` keeps the layout it built (#659); one energy and one site field in `sim.potts` (#722); the enumerations, annealers and Rust twins folded (#723 to #726) | #659, #695, #722, #723, #724, #725, #726 |
| 1.4 | The Potts lattice at its critical coupling renames the baseline (#704); the max-flow kernels in Rust, the winner in the package and the rest in `sandbox/` (#715) | #704, #716 |
| 2.1 | Canonical control problems with an optimum written a second way (#702); tabular Q-learning and SARSA disagreeing in the documented direction (#703); the sizing harness (#701); a `Learner` seam and `TreeEnvironment` (#705); the N-dimensional Potts environment and its cluster-move controls (#706) | #701, #702, #703, #707, #708, #709 |

Milestones 1.3, 1.5, 2.2, 2.3, 3.1 and 4.1 did not move.

## 2. Consistency audit

Each row names the two texts, or the text and the tree, that disagree.
**Fixed** rows are edited in this pull request; the rest carry a ticket.

| Text A | Text B or tree | What disagrees | Outcome |
| --- | --- | --- | --- |
| `STATUS.md` summary row "Stage 3 Research extensions" | `STATUS.md` section "Stage 5 --- Research Extensions"; `ROADMAP.md` Stage 5; Stage 3 is Surrogates & Bounds | the row labels Stage 5 as Stage 3 beside the 3.1 row | Fixed |
| `ROADMAP.md` Milestone 1.4 names ICM; `STATUS.md` 1.4 records it not started | `TICKETS.md` queues #176 under Milestone 1.5 | the last review moved the bullet the wrong way | Fixed |
| `ROADMAP.md` Milestones 2.2 and 2.3 | `STATUS.md` has a summary row and no section for either | two milestones without a ledger | #683, widened |
| `STATUS.md` §0 row cites PR #49; 1.2 row cites PR #74 | no commit names either; history starts at PR #79 | unverifiable rather than false | footnoted at the next release |
| `STATUS.md` names `learn.Environment` twice | the class is `learn.environment.Environment`; `learn/__init__.py` exports nothing since #727 | a symbol that does not resolve | Fixed |
| `STATUS.md` "nine problem statements", "nine sketch files" | the guard's tuple has 11 stems; 12 problem sketches under `docs/tex/` | stale by two and three | Fixed |
| `STATUS.md` "twenty-one QA scripts", "cite nineteen" | `qa.manifest.FIGURES` is 23; the two documents cite 23 | stale by two, and every figure is cited | Fixed |
| `STATUS.md` §0 row "1,528 entries, 151 modules" | 1,743 entries (167 modules, 284 classes, 656 functions, 636 methods) today; four other counts in the file, each its PR's | the summary row is the one live figure | Fixed, dated |
| `ROADMAP.md` and the `STATUS.md` 1.4 row: "Max-Cut with a certificate" | `STATUS.md`'s body: no SDP solver, Burer--Monteiro; #334 queues the SDP bound | the row reads as landed where the body says weaker | Fixed, qualified |
| `ROADMAP.md` Milestones 1.4 and 2.2 | every other milestone carries a *Validation* bullet | a deliverable with no referee stated | Fixed |
| `TICKETS.md` queues Q-learning and SARSA | `STATUS.md` 2.1 records both landed (#703, `learn/tabular.py`) | landed work queued | Fixed, deleted |
| `TICKETS.md` #596 "step 0 has landed" | `STATUS.md` records the Potts-lattice arm landed too (#704) | understated | Fixed |
| `TICKETS.md` cites #594 as #593's blocker | #594 landed (#700); the CRC is in `sim/elementary_codes.py` | a closed blocker cited | Fixed |
| `TICKETS.md` has no bullet for #682 to #686 or #688 | the 2026-09-16 review filed all six; five are open | the file claims to collect the open work | Fixed, six bullets |
| `ROADMAP.md` tier counts 368 / 2,860 / 2,969, dated 2026-09-16 | 392 / 3,062 / 3,159 today | two days old, three counts low | Fixed, re-measured |
| `DEV.md`, `INSTALL.md` and a test docstring name `test_oxiphylo_bindings.py` | the file is `tests/test_oxi_snakes_and_ladders_bindings.py` | a path that does not resolve, in three places | Fixed |

The guards pass on the fixed text: `test_planning_documents_agree` and
`test_repository_links`, 6 tests. The milestone guard compares heading sets,
so `STATUS.md`'s missing 2.2 and 2.3 sections pass on their summary rows;
that is #683's finding and the reason it stays open.

## 3. Quality

The tiers on `266cb65`, collected with `pytest --collect-only -q` over `tests/regression`, the directory CI runs; `tests/benchmarks` adds 399 (34 of them `release`) and is not counted, as the 2026-09-16 figures did not count it:

| Tier | 2026-09-16 | 2026-09-18 |
| --- | ---: | ---: |
| `critical` | 368 | 392 |
| CI tier, `not release and not stress and not key` | 2,860 | 3,062 |
| collected | 2,969 | 3,159 (3,089 under the `-m "not release"` addopts; 70 `release`) |

Coverage, from one run of the regression tier with a context per test on
the #733 tree, the three guards `infra/coverage_recut.py` now holds:

| Guard | Every package | `qa` exempt | `search` | Floor |
| --- | ---: | ---: | ---: | --- |
| gate, `--cov-fail-under` | 94.33% | | | 90 |
| judged, `end2end` or `oracle` | 77.93% | 82.68% | 86.18% | 82.6, `search` 86.1 |
| unjudged, the complement | 89.12% | 89.25% | 88.66% | 89.2, `search` 88.6 |

Of 942 public callables, 612 are entered by a judging test, 173 by a
self-check alone and 157 by no test; 955 statements are reached by no test.
The oracle ladder (#734): 416 `oracle` tests in 141 files pin the exact end
of every problem's ladder, and 22 rungs are pinned to nothing below them.
The overlap audit (#735, #738): of 1,682 test functions, 595 sit in 227
within-module clusters whose reached statements coincide; one asserts a claim
its neighbour asserts, ten are implied by a neighbour, 46 repeat a
computation, 171 are distinct. The 18 touched modules take 86.3 s against
103.0 s after the streamlining.

## 4. Problem statements

Eleven statements carry the five labelled parts by the guard; unchanged since
#679 apart from #710's request for a statement and a catalogue row for the
algebraic codes, which is open. Not re-audited here.

## 5. Frameworks: validation, extension, replacement

Left to the release audit, as the review ticket says. No framework was adopted
or declined since #679.

## 6. Gaps: what the roadmap promises and the tree lacks

- **The relaxed gradient estimators' bias and variance against the exact
  gradient** (`ROADMAP.md` Stage 5). `STATUS.md` claims exactness at the
  corners and nothing along the path; no ticket carried it. Filed as #739.
- **The coupled model past enumeration**: emission-parameter recovery and
  state-sequence decoding at the key instance (`ROADMAP.md`'s coupled
  requirements). `STATUS.md` records both "not measured"; `TICKETS.md` queued
  only the spectral seeding (#554). Filed as #740.
- **Problems the roadmap does not name.** The tree carries the turbo code,
  the count-emission mixture and, in `sandbox/`, polar and the algebraic
  codes; Milestone 1.1's text names none of them. `STATUS.md` recorded the
  same silence for 35 modules under #664. Fixed here: the milestone names
  them.
- CUDA and Metal dispatch (`ROADMAP.md` Milestone 1.2) is CPU only and queued
  as #280. Covered.
- Applicability: the 13 *untested* cells are one real gap (`planted_glass /
  samplers`), two out of scope at the instance (#666), three blocked on #669
  and seven refereed at another instance; the mark does not distinguish them.
  #684 narrowed accordingly.

## 7. Opportunities

Each row names the referee the tree already carries, so the proposal is a
test to write and not a validation to invent. Checked absent by `grep` over
`python/`, `docs/tex/`, `PROBLEMS.md` and `ROADMAP.md`.

### 7.1 Problems adjacent to the catalogue

| Proposal | Extends | Referee in the tree | Milestone |
| --- | --- | --- | --- |
| Ascertainment-corrected binary characters (restriction-site, Lewis Mk) | `sim.simulate.simulate_alignment` at `k = 2` | `likelihood.brute_force.brute_force_log_likelihood` for the constant-pattern mass; `likelihood.hadamard.hadamard_conjugation`'s two-state closed form | 1.1, 1.3 |
| Covarion: a hidden on/off rate switch, alphabet `k × 2` | `sim.gtr.gtr_rate_matrix` | the brute force at the switching-rate-to-zero limit, a two-component rate mixture | 1.1 (#107 is the umbrella; #323 is across sites) |
| Molecular clock: ultrametric lengths and the clock/non-clock likelihood ratio | `sim.tree.balanced_tree`, `opt.constrain` | the nested-model inequality on `likelihood.pruning.log_likelihood`; `search.statistics.chi_square_p_value` | 1.3 |
| Random-field Ising: quenched Gaussian field, ferromagnetic coupling | `sim.potts.spatio_only_field`, `sim.graph.lattice_graph` | `search.maxflow.ising_ground_state`, exact in polynomial time for this class; `enumerate_potts` for `log Z` | 1.1, 1.4 |
| Zero-field 2-D Ising at Onsager's free energy, as a canonical instance | `sim.canonical` | `likelihood.potts.strip_log_partition` and `enumerate_potts` against the published per-site free energy | 1.1, 1.2 |
| Hidden semi-Markov model, durations to a cap | `sim.hmm`, `opt.hmm` | `likelihood.hmm_paths.enumerate_hidden_paths` over the (state, remaining duration) expansion | 1.1, 1.2 |
| Factorial HMM: two chains, one gated emission | `sim.hmm`, `sim.spatio_sequential.gated_log_density` | `enumerate_hidden_paths` over the product chain; `enumerate_spatio_sequential` as the coupled analogue | 1.1 |
| Poisson single-channel count mixture | `sim.emission_mixture` with `emissions.PoissonEmission` | `likelihood.mixture_assignments.enumerate_mixture_assignments` | 1.1, 1.3 |
| Steane [[7,1,3]] from the Hamming(7,4) check matrix | `sim.css.CssCode.from_parity_check`, `sim.elementary_codes.hamming_code` | `likelihood.css.error_cosets`, exact degenerate ML over 128 errors; distance 3 external | 1.1 |
| Toric or rotated surface code, `Hx ≠ Hz` | `sim.css`, one sector today | `error_cosets` per sector once #362's two-matrix generalization lands | 1.1 |
| BCH over GF(2^m) at a second rate | `sim.galois`, `sim.reed_solomon` | `likelihood.ldpc.exact_decoding` below 24 bits; `hamming_code` is `t = 1` | 1.1 |
| Irregular or protograph LDPC ensemble; a rate-1/3 turbo | `sim.ldpc.gallager_code`, `sim.convolutional.octal_taps` | `likelihood.ldpc.erasure_threshold`; `sim.capacity.capacity` as the bound | 1.1, 1.2 |

### 7.2 Algorithms for existing problems, each placed on its ladder

| Proposal | Extends | Pinned against | Milestone |
| --- | --- | --- | --- |
| Tree bisection and reconnection | `search.topology.spr_neighbours` | `enumerate_topologies` (the image is a subset; reachability at 5 to 8 taxa) | 1.4 (#329 is multi-SPR) |
| Pseudo-likelihood estimation of `(J, h)` on a lattice | `opt.potts.PottsLatticeObjective` | `enumerate_potts`'s exact ML at CI size; planted recovery inside `opt.fit.covers` | 1.3 |
| Worm update on the Ising lattice | `search.potts_mcmc` | `enumerate_potts` with `chi_square_p_value`; autocorrelation time against Swendsen--Wang | 1.4, 1.5 |
| Wang--Landau density of states | `search.potts_mcmc`, `opt.schedule` | the exact density by enumeration; `strip_log_partition` for the `log Z` it integrates to | 1.5 |
| Annealed importance sampling as a `log Z` estimator | `opt.hmc.anneal`, `search.tempered` | `strip_log_partition`; the one-sided Jensen bound | 1.5, 1.2 |
| BP-guided decimation for the Potts MAP | `likelihood.belief_propagation` | the enumerated ground state; `maxflow.ising_ground_state`; `search.tightening.dual_bound` as certificate | 1.4 |
| Ordered-statistics decoding, and BP-OSD on the CSS code | `likelihood.ldpc.decode`, `likelihood.css.decode_syndrome` | `ldpc.exact_decoding`; `css.error_cosets` (0.0703 against the decoder's 0.0727 at [[16,2]]) | 1.2, 1.4 |
| Gallager A/B bit flipping; the BEC peeling decoder | `likelihood.ldpc.DecodingAlgorithm` | `exact_decoding`; `erasure_threshold` for the peeling threshold | 1.2 |
| Normalized and offset min-sum | `likelihood.ldpc.DecodingAlgorithm` | sum-product and `exact_decoding` at matched iterations | 1.2 |
| Variational Bayes for the mixture with component pruning | `opt.mixture.expectation_maximization` | `mixture_log_likelihood` bounds the ELBO; `enumerate_mixture_assignments` at CI size | 1.3 |
| Online EM on the mixture and the HMM | `opt.mixture`, `opt.hmm.baum_welch` | the same fixed point after a full pass; `opt.budget.compare` at equal evaluations | 1.3 |
| Split-merge moves over mixture components | `opt.mixture.MixtureFit` | `enumerate_mixture_assignments`; `optimal_clustering_cost` | 1.3, 1.4 |

## 8. Blue sky

| Proposal | Referee | Standing |
| --- | --- | --- |
| Survey propagation on the planted spin glass | the planted ground state and enumeration at CI size; nothing above it | filed as a candidate only when a referee past enumeration exists |
| Phylogenetic networks: one reticulation beside the tree | none: the brute force and Robinson--Foulds are tree-shaped | referee absent; not filed |
| Learned belief propagation for the CSS decoder | `likelihood.css.error_cosets`, exact degenerate ML at [[16,2]] | the classical LDPC half is in `docs/blue_sky.md`; this half is filable |
| One policy over `sim.factor_graph` conditioned on the problem, amortized across problems | each problem's own optimum; the transfer claim itself has none | #414 is transfer across sizes; the cross-problem claim waits for a referee |
| Kramers--Wannier duality as an exactness check at the self-dual coupling | `strip_log_partition` at matched widths either side of the map | filable, cheap, and a second closed form beside Onsager's |
| Region-graph free energy as a macro-move | enumeration at CI size; nothing above | declined under #689; a re-opening, not a gap |

## 9. Counts re-measured

| Count | Document said | 2026-09-18 | How |
| --- | ---: | ---: | --- |
| `critical` | 368 | 392 | `pytest --collect-only -q -m critical` |
| CI tier | 2,860 | 3,062 | the same, `-m "not release and not stress and not key"` |
| collected | 2,969 | 3,159 | the same, no marker |
| package modules | 151, 150, 149, 139 in four places | 167 (158 without `__init__.py`) | `git ls-files 'python/snakes_and_ladders/*.py'` |
| API-map entries, modules included | 1,528 | 1,743 | `infra.api_map.counts` |
| package lines, Python / Rust | 33,900 / 1,385 at 0.5.0 | 58,520 / 5,043 | `git ls-files … \| xargs wc -l` |
| QA figures, cited | 21, 19 | 23, 23 | `qa.manifest.FIGURES`, `cited_stems` |
| open tickets | 58 at #381's plan | 191 at the read | the issue list |

## 10. Follow-up tickets

- #739 --- the relaxed estimators' bias and variance against the exact gradient.
- #740 --- the coupled model past enumeration: recovery and decoding at the key instance.
- #683 --- widened to Milestone 2.3.
- #684 --- narrowed: one gap, twelve marks to relabel.
- #279 --- six labels on open tickets that `.github/labels.yml` does not define, found by #736's guard.
- #710, #682, #685, #688 --- open from the last review, now listed in `TICKETS.md`.
